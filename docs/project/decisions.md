# v0.2 决策记录

## D001 — 2026-09-17：以稳定性和维护成本为首要目标

来源：用户明确反馈“功能太多，流程不稳定、难以维护”。

决定：新 v0.2 推荐采用稳定重构范围。旧 v0.2 的功能进入待办，不凭历史已完成状态自动恢复。新增能力需要说明具体收益与验收成本。

## D002 — 2026-09-17：一个总控，按任务建立开发 session

来源：用户提出并指定当前 agent 担任总控。

决定：并发写入任务最多 1 个；任务结束归档事实，下一项使用新 session。模块和 session 不永久一一对应。复核较大改动时独立进行。

## D003 — 2026-09-17：从精确 rc.6/rc.5 组合重建

来源：当前 Git、pins 和已核实源码。

决定：工作台从模块化 `4eef8bf` 开始，业务引擎从 `8e00b334` 开始。旧本地 dsh reader 的未提交修改保留；远端引擎 main、旧 v0.2 完整包和历史输出均不作为当前基线。

已执行：独立检出引擎 `v0.1.0-rc.5` 并确认 HEAD 与 pins.sourceCommit 相等；没有修改业务代码或安装实例。

## D004 — 2026-09-17：结构重构与行为变化分开验收

决定：结构拆分保持 API、数据格式和运行时基线；可复现缺陷另开修复任务。同一任务不同时更换 DSH、认证、数据库格式和阅读流程。

## D005 — 2026-09-17：恢复要覆盖三个状态域

依据：旧升级说明与验收脚本的范围审查。

决定：文献库/资产、DSH 会话、handoff 绑定及任务回执分别验证。只备份 SQLite 或只检查 ZIP 存在不能宣称完整可回退。跨格式迁移另立任务。

## D006 — V02-001 启动确认

来源：用户先要求确认首个任务范围，随后明确回复“启动”。

决定：仅执行隔离验收与问题定位；允许补充验收脚本、测试样例和报告。所有产品代码修复另立任务，包括模块化引入的加载或路径回归。本次启动不授权后续开发任务自动开工。

## D007 — V02-001 诊断验收与 V02-002 就绪

依据：交付提交 `86d7833`、候选/rc.6 原始 JSON、夹具实现和总控差异复核。

决定：接受 V02-001 的验收与归因交付，将资料集成到总控分支；产品仍有已知问题，main 和发布状态不变。F1 进入记录冲突范围，F2 进入完整恢复范围。两项不阻断只做源码、接口、构建来源与测试基线审计的 V02-002；该任务设为 ready，尚未启动。002 不负责修复 F1/F2，也不直接开启全面重构。

## D008 — 自动推进与独立任务并行

来源：用户明确要求“你自动搞吧 弄个loop 002验收通过自动开启003 甚至互不影响的任务可以开启并行”。

决定：总控可持续创建、复核、集成和继续当前 v0.2 路线内的开发任务，无需逐项请求启动确认。每 10 分钟唤醒当前总控任务，最多两个独立开发任务并行，合入串行；queued/reserved 也占名额。该授权取代 D002 的单写入上限，以及 D006 中不自动开启后继任务的旧安排；001 的只诊断范围仍保留。

先派发 002 和只写文档的 006A；003 的实现依赖 002 审计通过。跨任务写入路径或接口存在冲突则串行。任务失败不自动推进依赖链，先归因、返工或建立最小修复卡。已知 F1/F2 不因资料集成而关闭。通知只针对实质进展、完成、失败或必须用户参与的事项，状态无变化保持安静。

## D009 — 接收 002/006A 诊断与设计，保留未解决事项

002 来源、接口与实际测试基线审计通过；额外导航合同测试失败 F3 另建 002B，不能宣称所有测试全绿。006A 设计集成，DSH 导入/禁重放等仍需能力验证，不直接实施完整恢复。003A 和 002B 文件与环境独立，可并行，合入后做组合验证。

## D010 — F1 的最小内容冲突合同

三个个人字段采用 B（成功导出基线）/E（Excel）/D（SQLite）逐字段三方比较，基线随工作簿隐藏表原子发布，不改 schema 4。无编辑不回滚 DB、单边编辑可导入、同值幂等、双边不同保留两端并明确 pending；任何冲突不部分导入、不覆盖原书。无基线旧书有差异则保守保留，不用 mtime 猜新旧。修复限 Excel 模块及测试，安装制品复验前不关闭 F1。

## D011 — 接收 F1 源码修复与 F3 测试修复，进入安装制品门槛

003A/002B 已串行集成到引擎 `4e5e6b964ddb8b823be17fb61774f8386bb0ba98`，总控组合复验通过。F3 关闭，F1 必须经过 003B 同一制品安装场景复测才关闭。003B 内部 A/B npm 开发候选命名 `0.2.0-dev.1`，Python `0.2.0.dev1`，只作本地制品身份；不发布、不打 tag，不冒用旧 rc.5，不变更宿主/运行时/schema。候选来源和报告提交分开追溯。

006B 可并行做固定 DSH rc.7 的 R1 取证；依赖 006A/002，独立目录/实例/端口，仅脚本和证据。不可直接实施恢复或使用 003B 尚未验收的 pins。F2 继续开放。

## D012 — 修正候选任务元数据范围，接收限定 R1 取证

003B 的 A 制品来源及回归通过可独立接收；B 安装未过，不能解除 003/004 依赖。catalog 必须随 pins 同步四个引擎身份字段，原卡漏列写入范围由总控负责修正。003C 从已交回的 B 提交续接，复用同一已校验 A 制品，不绕过校验器、不额外升级功能。003B 状态 needs_followup，dispatch 已结束，不再占开发名额；仅 003C 完整验收后可以结案。

006B 接受固定 DSH rc.7 文本会话与队列的限定可行性证据；恢复门禁、媒体与插件状态未验证。后续 R2-R4 不提前自动实现，仍服从主路线依赖和能力限制。

## D013 — 数据保护安装证据与用户冲突反馈分别验收

003C 已证实 dev.1 安装中的三方比较/整批保护/重启行为；但实际文献详情不呈现后端冲突详情，F4 为当前可复现问题。接受数据保护子结论，不豁免用户可见原因/字段要求，不关闭 F1 或提前启动004。003D只补最小客户端说明，不加编辑器/自动取舍/新作业协议；以A npm+B dev.2新制品复验，Python模块保持dev1且逐文件确认无差异。B候选在隔离分支待最终复核，历史失败与partial结果保留。

## D014 — 接受 dev.2 冲突闭环，004 先验证恢复合同

003D 的客户端差异、制品来源、安装数据保护与真实浏览器提示已经独立复核；F1/F4 关闭，A/B 候选链串行集成，F2 继续开放。003B/003C 的原失败记录不改写为原本成功。当前只进入 004A 合同诊断：默认阶段适配器抽离是候选方案，不是强制工作；依据真实进程恢复和副作用证据决定后续最小修复/结构卡，避免为模块化增加无收益改动。005 的导航观察只列核查项，尚无回归归因。

## 仍待决定（当前）

后续存储、精读作业、Reader 的范围是候选而非必须全面重构；按基线结果缩减、合并或取消，保留可运行的最小改动。

- v0.2 是否必须新增产品内单篇 chat、Figure 讨论或其他能力：当前推荐不作为首版门槛。
- 引擎内部是否需要额外接口层：以 002 职责地图和 004 合同证据决定，不以物理拆文件为目标。
- 真实模型与论文质量样例、发布平台范围：在对应验收任务中明确，不用合成结果代替。
- 开发组合何时合入 main：完成后续恢复、用户使用与最终制品门槛后决定。
