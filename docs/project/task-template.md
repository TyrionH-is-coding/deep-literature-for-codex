# <编号>：<一项可观察的完整行为>

- 状态/所属工作域/模块/负责人真实任务ID：
- 仓库/baseCommit/contextCommit/分支/worktree/依赖：
- 风险：R0文档 / R1局部 / R2数据、权限、并发、协议、恢复或安装。
- 工作量检查点：默认90分钟，预期首份证据；最多两轮修订后复盘方法（不自动通过/扩范围）。

## 目标与边界

一个具体触发条件、当前表现及期望结果；列允许路径、必要调用方、冻结接口和非目标。同一行为的实现、接入和验收尽量一张卡完成。范围外发现先分类，开发者不自动另开任务。

## 最小上下文

项目当前入口、目标模块公共入口、直接相关实现/测试和必需已核实结论；不加载全部历史聊天与日志。

## 验收与演示

- 可观察结果/演示步骤/退出标准：
- 本卡相关测试、必要失败路径、总控独立复核点：
- 可复用既有证据（精确源码/环境）及必须补验的差异；安装/真实模型边界：
- 超时/未知/连续失败时的下一步与回退条件；持久数据恢复另列：

## 交接（约一页，详细证据用索引）

- 源码/制品提交、用户可见结果、接口或格式变化：
- 实际命令/退出码/失败/跳过/未验证项；原始日志路径、SHA256与保留位置：
- 遗留问题：发布阻断 / 本卡必要修复 / 后续改进，附影响与归因：
- startedAt/reviewAt、实际返工轮数（未知记未知）；总控填写integratedAt/集成提交：

开发者交回 review，不能自行标记 integrated；不把被测来源不同的结果拼成一次通过。
