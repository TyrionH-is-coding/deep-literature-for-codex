# V02-002：固定引擎源码、接口和测试基线

状态：ready；V02-001 已复核，用户已授权总控自动启动本项及验收通过后的后继任务。实际派发状态以 tasks.json 为准。

## 目标与范围

在独立引擎仓库的 `8e00b334389cd90721b7d404013a07687753aa5c` 上，登记逻辑模块、公共合同、构建来源和测试分组。只改开发文档、必要的测试分组/构建核验工具；本任务不搬迁业务实现。

可用源码目录为 `C:/Users/15694/Documents/ChatGPT/deep-literature-engine`，但开始时必须重新核对 Git 身份并创建任务 worktree。转为 ready 时分别登记引擎 `baseCommit` 和承载任务卡的工作台 `contextCommit`，为开发 session 提供对应的最小资料包。工作台桥接契约参考 `docs/modules/engine.md`，代码审计结果参考 [引擎基线审计](../engine-baseline.md)。原 dsh reader 目录不是任务目录。

## 必须固定的合同

V02-001 的输入事实：旧 Excel 覆盖新 SQLite 记录（F1）和整实例恢复范围缺口（F2）均在原始 rc.6 对照复现，见 [总控复核](../evidence/V02-001-review.md)。本任务登记相应接口、现有测试和缺口；F1/F2 的修复另立任务，不夹带进入引擎基线审计。真实外部服务质量和完整后台流水线仍未验收。

- 数据库 schema 4 与现有升级路径；只在合成临时库上实例化服务。
- `ReadingPipeline.start/advance/inspect`、`reading-pipeline-v1` 及五个现有阶段。
- 已有翻译、审核、MinerU 规范化、generation manifest 格式及旧产物读取。
- TypeScript 宿主调用入口、scope 传递与后台任务恢复需要的状态文件。
- 运行版本、源码提交、构建命令、发布包摘要分别记载。重新构建压缩包字节不一定相同，不能以版本号相同代替来源证明。

## 验收和交接

1. 记录依赖/工具版本及准确命令，执行现有 Python/Node 基线测试并区分通过、失败、跳过和外部依赖缺失。
2. 给出逻辑边界、当前实现、入口、依赖、测试映射；模块名由总控确认后登记，不一次迁移全部目录。
3. 审计固定包与源码的对应关系和构建过程，保留真实证据；无法证明的部分明确列为缺口。
4. 将默认阶段适配器抽离整理为下一张单目标任务卡草案，保持 schema、外部入口和 `background_store` 不变。
5. 交回提交、验证记录及剩余缺口；没有测试结果前不能宣称引擎基线通过。

该任务完成后，后续 workstream 必须先拆出一个可验收的开发任务，才可设为 ready。

## 本次派发约束

- 使用独立引擎 worktree `C:/Users/15694/Documents/ChatGPT/deep-literature-engine-v02-002`，分支 `codex/v02-002-engine-baseline`，源码基线仍为上述 `8e00b334`。任务资料从台账固定的工作台 contextCommit 读取；不能读可变分支当作固定任务包。
- 可写路径：引擎仓库 `docs/codex-v02/` 和新增的 `scripts/v02-audit-*` 核验工具。原有生产源码、包版本、依赖锁文件、数据库格式不改；安装开发依赖产生的忽略目录不作为提交内容。
- 开工先校验 Git 身份，再写 `docs/codex-v02/V02-002-receipt.json`，记录任务 ID、实际 worktree、baseCommit/contextCommit、时间、phase；能可靠取得自己的真实 Codex task ID 时记录，取不到保留 null。总控可能通过该回执弥补应用列表暂不可见的情况。
- 主报告为 `docs/codex-v02/V02-002.md`，原始测试和制品来源证据同目录保存。使用合成临时库、锁定依赖与本地夹具；不调用真实模型/MinerU，不打开用户文献库。
- 交付时提交允许范围内的变更，回执 phase 改为 review；总控验收后决定 integrated。缺少实际测试、未核实源码对应关系或未归因阻断问题时，不可写为通过。
- 003 的下一张卡优先聚焦 F1 的旧快照冲突合同与最小修复。默认阶段适配器抽离只列为后续候选，不能越过数据保存问题先做大范围重构。
