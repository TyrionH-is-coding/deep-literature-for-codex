# V02-003C：同步候选模块身份并完成安装验收

单一目标：解除 003B 因任务范围遗漏而遇到的 catalog 身份不一致，完成它尚未执行的打包/隔离安装/F1 与主流程验收。用户已授权总控自动最小修复，无需再次确认。

## 已核实的输入

- 新 B 工作树 `C:/Users/15694/Documents/ChatGPT/deep-literature-v02-003c`，分支 `codex/v02-003c-candidate-acceptance`，基线 `8c37fb13d7c6816c1de067d72ef8fef091097ac3`（003B 已终止交付，未合入 B 总控）。
- 固定任务资料 contextCommit 见总控台账，从 `C:/Users/15694/Documents/ChatGPT/deep-literature-for-codex` 用 `git show <contextCommit>:docs/project/tasks/V02-003C.md` 读取；新 worktree 的旧台账不是当前派发状态。
- A 已独立复核集成：报告 `e3f58d1710413b5c6340fe55974a227b97ee8373`、候选来源 `ca02db6beb2cf45ad0fd69618313971d0a346c55`。tgz 在 `C:/Users/15694/Documents/ChatGPT/deep-literature-engine-v02-003b/dsh-external-dsh-scientific-reading-0.2.0-dev.1.tgz`，SHA256 `fdce0ada390e2f27615c6b6050dbf7ef83c871d6a6304c4235e4414f667c8930`。
- 只读核验并复制该固定制品到本任务 inputs；禁止改 A 源码/制品/依赖或写上一任务目录。已核实 A 全量 515 pass / 3 skip 与默认 Node/build；相同制品不需要重复全套 A 测试，安装产物复验必须进行。

阅读 AGENTS、本卡、003B 原卡、003B 总控复核及必要安装/验收入口。先核对 Git 状态，写 `docs/project/evidence/V02-003C-receipt.json`（完整基线/contextCommit/目录/分支/时间/phase，ownerSession 无可靠 ID 则 null）。

## 写入范围

仅改 `src/modules/catalog.json` 中 `modules[id=engine].version` 和 `externalEngine.version/sourceCommit/sha256` 四个身份字段，全部取当前 pins；不得动模块依赖、ownedPaths、校验器或保护断言。可新增 `scripts/acceptance-v02-003c*`、`scripts/fixtures/v02-003c/`、`docs/project/evidence/V02-003C*`；可写本目录独立 inputs/outputs/.local。已确定的 package/锁/pins/版本/schema/生产逻辑均不改。遇到另一个真实越界需求，保存实际证据交回总控；不得用绕过校验来完成。

## 验收和交接

1. 同步四字段，运行 modules check/impact 与受影响模块及调用方测试。提交 catalog/脚本，固定新的 B 候选来源。不要把上一任务失败日志改为成功。
2. 使用现有 package-release 脚本从干净 tracked source 打包校验解包，明确使用已核验本地 A archive；不虚构 URL。由解包程序安装到全新短路径合成 root，禁用个人 skill，运行 Node 22.22.2 / Python pins 对应解释器，记录安装版本、tgz/wheel/B manifest 与来源一致性。下载缓存只读复制，经摘要核对后使用。
3. 原 003B 卡第 4、5 条验收仍完整有效：实际安装的引擎/CLI/工作台验证旧书不覆盖新 DB、仅 Excel 编辑、双边冲突与整批不部分写、legacy、重试和两次重启、worker/界面可见状态及定位；合成主流程验证启动/身份/Reader/笔记/handoff。不得用源码 PYTHONPATH 冒充安装包，不以没有请求异常代替业务断言。
4. 复用 001 的样例意图可写新脚本；原始 001/003B 证据保留。F2 仍为库备份之外的整实例恢复缺口，不做完整恢复声明。真实模型/用户数据/真实凭据不在范围，网络仅本地模型夹具。
5. 交回 `docs/project/evidence/V02-003C.md`、专属原始证据、review 回执与完整 SHA，来源提交和报告提交分开。停止自身进程。总控决定集成、关闭 F1 与继续 004；禁止自行修改总控台账、合入 main、发布或打 tag。

当前只启动本卡一个写入任务；006B 的 R1 取证已完成，后继恢复实现没有派发。回退仅代码/元数据，不覆盖用户记录。
