# V02-002B：恢复导航合同测试的有效覆盖

状态：ready；用户已授权自动启动，依赖 V02-002。可与 003A 并行。

引擎基线 `d26cb9e88d0f7884db8e69a5a2d18286a62aaf83`；独立 worktree `C:/Users/15694/Documents/ChatGPT/deep-literature-engine-v02-002b`，分支 `codex/v02-002b-navigation-contract`。固定任务资料由工作台 contextCommit 提供。

## 目标和写入范围

002 发现默认链之外的 `tests/navigation-contract.mjs` 首个断言期望缺少当前返回的 `journal` 与 `search_matches`，之后断言未运行。本任务核实现行合同，修复过期测试并纳入默认测试链，不改变生产返回字段。

只可修改 `tests/navigation-contract.mjs`、`package.json` 的测试 scripts，以及新增 `docs/codex-v02/V02-002B*` 资料；不改生产 TS/Python/client、依赖/锁文件/版本、Excel 测试或 003A 的文件。

## 步骤与验收

1. 核对基线并写 `docs/codex-v02/V02-002B-receipt.json`，记录完整基线/contextCommit、目录/分支、时间、phase；无法可靠取得真实 task ID 则 ownerSession=null。
2. 对照现行 `src/routes.ts`、相关默认导航测试与 002 原始日志，复现首个失败。只调整已经证实过期的期望，不能删除/放宽脱敏、参数校验、方法/路径、Reader/资产和工具返回保护断言。
3. 跑完整 navigation-contract；首个断言之后出现的新失败逐项归因，不可假定都只是陈旧期望。若发现真实生产问题或无法判定的合同差异，保存证据并报告阻塞，另立修复卡；不改生产代码来让本卡通过。
4. 在 package.json 默认离线测试链纳入该检查，执行默认 Node 链及必要 build。Python 只为该 Node 测试提供本地夹具；不得调用真实模型/MinerU或真实用户库。
5. 记录前后结果、改动理由、命令/环境/跳过范围，交付主报告 `docs/codex-v02/V02-002B.md` 和同前缀证据目录，提交允许文件并将回执置 review。

此项与 003A 写入路径不重叠，各自使用独立依赖、构建输出和临时实例；不能修改共享总控台账或另一任务文件。集成后总控验证组合。
