# V02-003B：候选制品安装与 F1 原场景复验

状态：ready；依赖 003A、002B 总控组合复核。父工作域 V02-003。单一目标是证明已接受的 Excel 修复真实进入工作台安装产物，不增加功能。

## 固定工作范围

- B 工作台工作树：`C:/Users/15694/Documents/ChatGPT/deep-literature-v02-003b`，分支 `codex/v02-003b-installed-candidate`；baseCommit/contextCommit 见台账。
- A 引擎构建工作树：`C:/Users/15694/Documents/ChatGPT/deep-literature-engine-v02-003b`，分支 `codex/v02-003b-engine-candidate`；基线 `4e5e6b964ddb8b823be17fb61774f8386bb0ba98`。
- B 可改：`package.json`/`package-lock.json` 仅版本元数据，`src/modules/foundation/constants.mjs` 仅 VERSION，`runtime/pins.json` 仅 plugin 与 channel，`runtime/package-lock.json` 仅该本地插件的版本/integrity/相关包条目；`scripts/acceptance-v02-003b*`、`scripts/fixtures/v02-003b/`、`docs/project/evidence/V02-003B*`。inputs/outputs/.local 可存本任务独立未跟踪产物。
- A 可改：`package.json`/`package-lock.json` 仅自身版本，`engine/pyproject.toml` 仅自身版本；`docs/codex-v02/V02-003B*`、`scripts/v02-003b-*`。禁止改任何生产逻辑或依赖版本；遇到真实问题保存证据交回。
- 禁止修改主线/总控台账、现有用户安装或旧 dsh reader；禁止发布、打 tag、上传 release 或调用真实模型/MinerU。任务自己提交，不合入总控。

最小上下文：AGENTS、此卡、003A 总控复核、engine 模块说明、001 安装证据及测试脚本中需要复用的场景、现有构建/安装入口。先核对两个 Git 身份，写 B `docs/project/evidence/V02-003B-receipt.json`，记录两个目录/分支/完整基线/contextCommit/时间/phase；可靠 task ID 不可取得则 ownerSession=null。

## 候选命名和制品来源

本次只生成内部开发候选：A npm 与 B 版本 `0.2.0-dev.1`；Python wheel 用 PEP 440 对应 `0.2.0.dev1`。它不是正式 v0.2，也不得冒用旧 rc.5 摘要/版本。不得改 DSH rc.7、Node 22.22.2、Python 运行时 pins、数据库 schema 4 或 dataFormat。

先提交 A 版本元数据，构建 wheel/TS/client 并打 tgz，保留完整 A 来源 SHA 和制品 SHA。B pins 绑定这份 tgz；本地提供 archive，不填写虚构的远端下载地址，不保留指向旧 rc.5 的 plugin.url。如现有验证要求 URL 且不支持本地候选，记录精确阻塞，不放宽完整性校验。B 锁只更新本地插件自身记录，检查其他锁定包完全不变。B 先提交产品元数据/脚本再从干净 tracked source 打包；报告提交与候选来源提交分别记录，避免自引用。

## 验收

1. A 候选执行全量 Python 和默认 Node 离线/资产链及构建，依赖沿用 002 精确环境清单；记录跳过项及命令，不拿已有日志代替。使用独立依赖和构建输出，不写其他任务 worktree。
2. B 执行模块边界检查和受影响模块/调用方测试。版本变化导致硬编码测试失败只能报告，不能扩大允许范围改掉保护断言。
3. 使用现有 package-release 流程打包、校验与解包。由解包后的程序安装到本任务全新短路径合成 root，禁用安装个人 skill，不复用 001 实例。可复制已核验的下载缓存到合法初始化后的本任务 root；必须验证摘要。记录 Node 22.22.2 / Python pins 的实际解释器，安装的 A tgz/wheel/source 对应及 B manifest 来源。
4. 从安装产物的实际 CLI/工作台入口复验：导出未改旧书 → DB 三字段更新 → 刷新，DB 新值保持；只有 Excel 改动可保存；同字段双改 pending 且双方保留、整批不部分导入；legacy 有差异保留；重试/两次重启不绕过冲突。断言读取安装 site-packages，不能偷偷设置 PYTHONPATH 指向源码。确认 worker/前端可见的状态与定位信息，用本地夹具隔离外部调用。
5. 复用并适配 001 的合成主流程 smoke 检查安装启动/身份/Reader/笔记/handoff 不回归。保留 001 原脚本原始失败样例；新脚本不得把 F2 的库备份局限改成整实例通过。旧 F1 预期更新为本卡合同，历史/当前结果分开。
6. 关闭本任务启动的进程，记录制品绝对路径、SHA、两个来源提交、实际结果与未验证范围；交付 `docs/project/evidence/V02-003B.md` 和同前缀证据、review 回执，A/B 各自提交允许文件。总控决定 F1 / V02-003 是否关闭，本任务不得自行改台账。

与 006B 并行依据：006B 固定旧 B/DSH rc.7，只写独立探测脚本/证据，不消费本卡尚未完成的候选；双方独立实例、端口、依赖、缓存写入和构建目录。
