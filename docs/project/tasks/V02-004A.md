# V02-004A：固定精读阶段与恢复合同

目标：在已验收的 dev.2 引擎基线上，把精读阶段、持久状态与续接边界写成可核对的合同，用隔离进程和合成数据检查重复提交、失败续接、中断重启、取消以及已确认资产保护。先验证现行行为；只有证据表明需要结构调整或修复时，由总控另开单目标卡。

## 上下文与基线

- A 工作树：`C:/Users/15694/Documents/ChatGPT/deep-literature-engine-v02-004a`，分支 `codex/v02-004a-pipeline-contract`，base `6283c02a690ff42585f064c1f764b999baa97d7e`。
- B 总控资料：`C:/Users/15694/Documents/ChatGPT/deep-literature-for-codex`。contextCommit 以派发提示和台账为准，用 `git show <contextCommit>:<path>` 读取固定内容，不修改该目录。
- 应用默认目录及其自动工作树来自旧 `dsh reader`，不是任务源码。所有源码命令显式使用上述 A 工作树，不 reset、合并或修改旧目录。
- 先核对 Git 状态/完整 HEAD/分支；读 B AGENTS、session-protocol、本卡、003D-control-review，再读 A 的 `docs/codex-v02/V02-002.md` 中 Pipeline 合同与测试地图。按需读 reading_pipeline/models、worker、background store/launcher、full_read 服务与相关 CLI/路由调用方，不预加载全部历史方案。
- 开工写 `docs/codex-v02/V02-004A-receipt.json`：任务号、基线、contextCommit、分支/路径、时间、phase；无法取得真实 session ID 则 ownerSession=null，禁止用 clientThreadId 冒充。

## 写入范围与不变量

只允许新增 `docs/codex-v02/V02-004A*`、`scripts/v02-004a-*`、`scripts/fixtures/v02-004a/`、`engine/tests/test_v02_pipeline_contract.py`、`tests/v02-pipeline-contract.mjs`。可按实际需要使用其中一部分。测试默认链接入和生产修复均留后续卡，不修改生产代码、既有断言、版本、锁、依赖、schema、模型或 UI。不要为本卡物理搬迁阶段适配器。

保持 `reading-pipeline-v1`、阶段顺序、parent job ID、scope 校验、job JSON、SQLite schema 4、generation/资产合同及 CLI 有效退出码 0/2/3。依赖用 002 审计固定版本，独立 .venv/node_modules；可读取其他已验收工作树的缓存，不改其安装环境。仅用本卡临时库/本地夹具，不使用真实凭据、模型、MinerU 服务、个人论文或用户安装。后台子进程隐藏启动，记录 PID/实例身份并仅停止本卡进程。

## 验收内容

1. 产出一张紧凑阶段合同表：ensure_pdf、parse_mineru、translate_full、render_reader、schedule_derived_updates 各自输入、持久输出、成功/用户或 agent gate/失败状态、实际续接入口和副作用归属。区分 pipeline parent、background child、library 指针和正式资产，不只画调用图。
2. 检查同一输入重复 start/resume 的 ID、调用次数、文件与发布行为；明确并发 claim/重复提交的现行保证和限制。用真实入口或真实服务，外部服务可以受控替身；不要靠全替换 pipeline 的 stub 自证。
3. 在 parse/translation 的代表性边界注入失败与中断；持久化后启动新 Python 进程恢复。至少包含一个活跃任务的可控进程终止/退出和进程重新加载，证明旧 PID/claim 不被误认为有效。记录中断位置、状态/调用次数与最终资产 SHA，不能把重建同进程对象称作跨进程重启。
4. 验证已确认解析或翻译片段在有效续接时受到保护、不被无理由重复生成；不完整候选不能替换正式资产。区分合理重建（输入变化/未提交输出）和重复执行副作用，不能预设所有失败点都有 exactly-once 保证。
5. 通过现有支持入口验证取消排队/运行任务及后续续接的实际边界，避免取消其他任务或默默重放已取消操作。若引擎与 B handoff 拥有不同取消职责，明确分界与尚未验证部分；不能据底层测试宣称完整宿主 UI 取消已通过。
6. 跑相关已有 reading_pipeline_integrity、worker_full_read、background launcher/PID、MinerU gate/resume、pipeline_excel_end_to_end 以及 Node full-read-pipeline/reading-routes/restart-recovery 等必要合同测试；根据实际文件名选择，记录每条命令和真实计数、skip 与原因。不要求无关全量回归，不合计 Node 命令数和 pytest cases。

## 交付与退出

提交报告、可重放探针/有价值的新合同测试、逐项结果与来源/环境/原始日志、进程清理记录。报告须分清现有测试、新增测试、真实进程证据及未验证项。复现问题时保留失败断言/输出与最小触发条件，给出单目标后续建议；可交回 diagnostic-with-findings，不放宽断言、不隐藏失败、不自行扩大范围。

最终完整 commit 与 clean Git 状态、receipt phase=review、接口/格式无变化及撤回条件交总控。本卡通过是合同诊断交付通过；只有全部 004 退出条件满足且阻断发现已解决，总控才关闭 004。禁止自行领取 005、创建其他任务、修改总控台账、合并 main、打 tag、发布或升级用户实例。
