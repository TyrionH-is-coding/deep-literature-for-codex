# V02-006A：完整实例恢复的最小方案

状态：integrated（仅设计文档通过）。交付 `6a8f491` 已并入总控 `bf5adbc`；见[总控复核](../evidence/V02-006A-review.md)。完整恢复功能与最终制品验收尚未完成，F2 保持开放。

## 目标

定义一套最小的完整实例恢复方案与验收场景，明确文献库/资产、DSH 原生会话、handoff 账本三个状态域，以及实例身份、绝对路径与凭据的处理。产物是可交给后续实现任务的方案，当前不实现恢复功能。

## 基线与阅读

使用总控台账固定的工作台 contextCommit，在独立 worktree `C:/Users/15694/Documents/ChatGPT/deep-literature-v02-006a`、分支 `codex/v02-006a-recovery-design` 工作。先读 AGENTS、PROJECT、当前卡、auto-loop、releases/workflow/lifecycle 模块说明及公共入口、V02-001 F2 报告和原始备份条目；再只读相关实现。引擎参考固定 `8e00b334`，不能采用 002 未完成的改动或远端 main。

## 写入范围与约束

- 仅允许 `docs/project/design/instance-recovery.md`、`docs/project/evidence/V02-006A.md` 和 `docs/project/evidence/V02-006A-receipt.json`。
- 不修改产品源码、任务台账、PROJECT、依赖、格式或用户实例；不运行真实服务/模型，不处理真实凭据和文献内容。
- 启动先核对 worktree/HEAD/分支，再写回执：任务编号、实际目录、完整基线、时间、phase，真实 task ID 可取得则记录，否则 null。交付时将 phase 设为 review。

## 验收

1. 用精确源码路径及 001 证据列出各状态域当前保存位置、已覆盖内容和缺口；区分源事实与设计建议。
2. 给出一次最小备份/恢复的顺序、停机或一致性要求、清单/摘要验证及失败中止策略；恢复到独立目录，防止半恢复覆盖有效实例。
3. 明确实例 ID、路径、会话/分类绑定和未完成任务的重绑定规则。凭据不复制，说明恢复后需要重新配置的边界。
4. 给出故障注入与读回场景，包括缺文件、摘要错、版本不兼容、重绑失败、部分恢复与中断；这些是未来测试计划，不能写为已通过。
5. 比较最小可行方案与额外复杂度，推荐一个方案，并拆出单目标实现卡草案。后续正式实现和最终制品验收仍归 V02-006，不跳过依赖。
6. 只检查文档链接、事实依据和差异；没有运行产品测试就明确说明。提交允许范围内文件，交回完整 SHA、报告及剩余问题，由总控复核。
