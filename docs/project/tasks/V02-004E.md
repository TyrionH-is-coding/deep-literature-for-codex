# V02-004E：宿主失败时保留取消意图

单目标：修复004C-C1。用户提出取消后，宿主不可用/调用抛错不能使该意图在重启后丢失并恢复默认投递。本卡只修 B 的可靠意图，不声称 A detached worker 已停止，不实现整套新控制协议。

## 固定工作树

B `C:/Users/15694/Documents/ChatGPT/deep-literature-v02-004e`，分支 `codex/v02-004e-cancel-intent`，base `d78e48bbad317c92770b767390addd589b475038`。总控资料 `C:/Users/15694/Documents/ChatGPT/deep-literature-for-codex` 指定 contextCommit，用 git show 读取本卡/AGENTS/session-protocol/004BC-control-review。先核对Git状态和模块context workflow，写 `docs/project/evidence/V02-004E-receipt.json`。不使用应用默认旧 dsh reader，不能写总控或004D目录。

## 最小行为

1. 已有 checkedTask 的身份/范围校验后、任何外部 cancelTask 调用前，可靠保存 cancelRequested=true。保存失败则不开始外部副作用。宿主取消失败仍可向调用者报原错误，但持久意图必须保留，task/list/reopen/同键submit和默认dispatch继续抑制目标投递。
2. 失败后重复 cancel 必须能重试宿主撤回，不因已有标记直接假称成功。不把失败的宿主调用写成已撤回/已停止；允许保留上次真实取消结果但不能冒充本次成功。不要写凭据、原始stack等到用户状态。
3. 保持显式 dispatch(retryKey) / 成功新 resume/attach 的现行行为；旧 completed operation 重放不清后来取消，失败 resume保留意图。C3的清除时序另归总控，不在本卡新增A revision协议。
4. completed/failed真实终态与已提交Reader继续按现行合同呈现，不将后台running标成已取消。其他任务的队列/共享turn必须不受影响。A接口、worker和scope不变。

## 写入范围

生产仅 `src/modules/workflow/handoff.mjs` 直接相关 cancel 持久化/失败处理；`tests/v02-cancel-contract.test.mjs`、`tests/handoff.test.mjs` 中相应新期望和故障回归；docs/modules/workflow.md、changes/workflow.md；catalog仅workflow版本允许增至 `0.1.1`（此为行为修复追溯，不改 engine/其他模块）。新增专属 `scripts/v02-004e-*`、`scripts/fixtures/v02-004e/`、`docs/project/evidence/V02-004E*`。

不用新测试文件绕过登记；如确有必要新增则仅其workflow ownedPaths/tests登记也允许。不得修改产品版本、pins/locks、bridge/API、A源码、新状态schema或设计A stop接口。不能放宽004C其他保护断言。004D仅管另树发行身份，与本任务不共享实例、端口或可变协议。

## 验证与交付

- 先原样复现004C host failure当前缺陷，再将该测试改为正确持久意图期望并加入保存失败、外部失败、重复重试、重启、新/旧操作键、其他task不受影响的必要故障断言。
- 至少一次独立Node进程只读回本卡临时handoff后重新打开服务，证明意图在文件中而非同进程缓存；外部宿主可注入失败，本卡不冒称真实A取消验收。
- 执行 modules check/impact/test workflow 及桥接相关回归，准确记录计数/skip、原失败与最终结果。无生产接口变化也应检查原生helper合同未受影响，不要求重复启动真实A。
- 独立合成数据，无真实凭据/模型/用户实例；清理仅本卡进程和经验证的临时根。交回完整commit、接口/格式影响、测试及限制，Git clean、receipt review。不升级用户、合并main/总控、发布或派生任务。

该修复仅关闭C1，不关闭004A-G1；后台安全边界停止与类别工具守卫仍待后继卡。最终与004D候选组合还要验收，不把两套不同来源的结果拼成安装通过。
