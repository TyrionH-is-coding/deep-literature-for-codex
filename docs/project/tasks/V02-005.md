# V02-005：Reader 样例与首次完整流程演示

目标：用已验收 dev.4 候选，在独立实例完成“入库→补入 PDF→精读→Reader→保存记录→重启继续”的可复现演示，检查 Reader 来源/译文/图表/公式及现有操作入口。先验已有行为，未复现缺陷可直接验收结束，不强制增加实现层。风险 R2；首次 90 分钟检查点应交一个最小正常样例的进展/具体阻碍，最多两轮同类修订后复盘方法。

## 固定输入与写入边界

- B 工作树 `C:/Users/15694/Documents/ChatGPT/deep-literature-v02-005`，分支 `codex/v02-005-reader-demo`，base `ce5fd9aac20985f3f80ca675b8781e40006a227e`。contextCommit 为派发消息指定的总控文档提交；不要把应用默认旧 dsh reader 目录当作基线。
- A 只读参考 `C:/Users/15694/Documents/ChatGPT/deep-literature-engine`，提交 `ad00bc5a84110346801dbb6e98c8b9e19135287d`；安装生产来源 A `2dcd068bf3cbdf4b601351ab0bda639873bc0359` / B `572fee5c8fa28fd920221534ea28b3ce70051c0c`。
- 使用原 ZIP `C:/tmp/v004k/candidate-r1/deep-literature-for-codex-0.2.0-dev.4-win-x64.zip`，SHA256 `862e08c5ba85b399f6530d468298cfc7d3866675eae4b5ebe09e8b950610d105`，经原安装器安装到本任务独占 `C:/tmp/v005/` 下。不重建无变化制品；可只读核验/复制旧下载缓存，不启动/改写 004K 实例和证据，不安装个人 skill。
- 只写 B `scripts/v02-005-*`、`scripts/acceptance-v02-005-*`、`scripts/fixtures/v02-005/`、`docs/project/evidence/V02-005*`；大日志/截图/实例放 B `outputs/v02-005/` 或 `C:/tmp/v005/` 并列哈希索引。总控台账/PROJECT/本卡由总控写。
- 生产源码、依赖、版本、schema、打包器与既有测试初始不在修改范围；发现真实产品缺陷先报复现/影响/最小允许范围，由总控分类后同任务修订。夹具可自行修正，但不能用手写正式 Reader/库状态、替换安装引擎或弱化验证掩盖产品失败。

## 退出条件与早期演示

1. 开工核对 base/clean、AGENTS 与 engine/bridge 模块 context，写 `V02-005-receipt.json`（真实 sessionId、base/context、路径、时间、phase）。按需读 004K 总控验收/报告及可复用脚本，不载入全部历史记录。安装来源和实际入口核实即可引用 004K 制品/全集证据；新实例与新样例要有自己的实际运行证据。
2. 用最少的小型、自有合成 PDF 样例：一个完成完整正常链，必要时再补一个图/表/公式样例，不以数量为目标。优先复用 A `review/fixtures/formula-outline.json`、`figures-captions.json`、`superscript-text.json` 的相关内容并记录 SHA，不重造样例体系；补充渲染检查不冒充完整链。外部解析 provider、翻译/精读输入可合成并显式标明，但安装默认 worker、正式发布、Handoff/适配器和 Reader 必须是真实安装代码；翻译/复核遵守当前 gate 合同，不靠手改作业状态推进。
3. 正常链经已有可信入口建立题录/分类、attach PDF、推进到正式 Reader；核对 paper/source SHA/generation、资产清单及来源/译文内容一致。记录入口以现有 Excel 三字段为准：修改本任务合成 XLSX，使用安装引擎 `xlsx-import-user-fields`/`xlsx-refresh` 导入和刷新，再完整宿主 stop/start 核对同一文献的 Reader、记录、来源与进度保持；不能直接 UPDATE SQLite 代替保存流程。题录、精读、记录可用项目既有 Codex/CLI/工具入口，不预设网页笔记编辑器或停止/恢复按钮。浏览器已有“定位 Excel”可核实其实际反馈，桌面 Excel 交互若未操作则单列未验。
4. 用可用浏览器实际打开新实例 Reader/库入口，检查可读性、文字及译文、图片/表格/公式、已有目录/来源链接的实际交互，保存必要截图与入口说明。HTTP 成功不替代浏览器验收；没有的 UI 能力记录为实际边界，不补造按钮/主题。若浏览器能力不可用，明确未验项和下一步，不标 UI 通过。
5. 同一候选演示一条失败后的显式安全续接：优先复用 004K 的小型 provider 失败场景，在新样例核验现有可信入口如何查询状态、cancel→acknowledged→resumeStopped，并展示同 parent/source 实际进入合法下一 gate 或完成；仅恢复请求成功不算续接通过。沿用 004K 对 requested/ack、独立 child 和陈旧 running 的解释；如需复现 crash，只处理已确认身份的本任务进程，不能用 kill 代替用户停止。已有多别名/原生队列/两次重启等专项证据引用 004K，不再次扩大矩阵。
6. 对发现先给分类建议：发布阻断 / 当前目标必要修复 / 后续改进；未证实导航观察不直接立修复。真实模型、科学质量、F2 三域恢复、跨平台、额外主题/新对话功能均不由本演示声称完成。需要真实账户或用户判断时仅暂停受影响链并报告具体缺口。

## 交付与收尾

约一页 `V02-005-report.md` 加证据索引：固定源码/制品、样例来源与限制、逐步入口/预期状态、实际结果与截图、命令/退出码/哈希、未验项、问题分类、回退条件。先给可展示的最小正常结果，不先搭通用验收框架。源码未变不重复 A/Python/B 全集；若授权修复，按影响补验并更新制品身份。

交付提交与 clean 状态，receipt 置 review；保留演示实例/样例/必要日志，清理仅本任务确认归属的运行进程并给出以后打开的精确命令。不得发布/tag/合总控/main、改用户安装/文献库、自行派发后继。总控独立复核实际演示后决定解除 006 依赖；真实用户反馈仍不等于最终 007 已通过。

## 总控入口取证说明（2026-09-21）

API bind 创建的四个分类会话均为 blank、无首条 prompt/turn。固定宿主的空会话 UI 会隐藏标签栏和 active view、排除搜索；浏览器标题含分类名而正文为 New Session 并不表示会话丢失。`/sr` 只是提示去对话栏打开文献标签，不是独立库界面；输入未提交草稿也不会改变 composerPhase。当前只确认这一前置条件，未授权修改空会话产品行为或伪造内部状态。

在本卡原合成模型范围内，优先复用004K已有本地模型替身，以正式原生 prompt 完成一次不含工具调用的合成会话，再在真实浏览器进入文献标签、打开 Reader 并核验“定位 Excel”实际反馈。仅使用本任务实例、夹具和本地端点；不接真实账户/模型，不改安装包、手工缓存或数据库。新增准备限定为一个最小样例，首轮约20分钟检查点；若现有夹具不能直接复用或需要超范围实现，报告具体阻碍并保留库入口未验，不为此搭新框架。发送尝试失败导致界面展开不算正常会话/入口通过。
