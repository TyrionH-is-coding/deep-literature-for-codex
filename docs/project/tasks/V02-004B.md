# V02-004B：修复解析提交前中断后的安全续接

单目标：修复 004A-F1。同一 source 的解析任务在正式 parse 提交前崩溃，新的进程应能在现有身份、源文件、scope、锁与资产守卫下显式续接。不得通过删除 generation 或放宽全部校验来恢复。

## 固定上下文

A `C:/Users/15694/Documents/ChatGPT/deep-literature-engine-v02-004b`，分支 `codex/v02-004b-parse-recovery`，base `dcdb8e6cb23dd8329ceabd4487db0a79fae470ea`。应用默认旧 dsh reader 不使用。B 总控 `C:/Users/15694/Documents/ChatGPT/deep-literature-for-codex`，用派发 contextCommit 读取本卡、AGENTS、session-protocol、004A-control-review；只读 B。

开工核对完整 HEAD、分支、状态，写 A `docs/codex-v02/V02-004B-receipt.json`（base/contextCommit/path/time/phase/ownerSession，真实任务 ID 不可得则 null）。读取 A 004A 报告、失败探针和 `_stage_workspace` / MineruParseService running checkpoint 相关源码，再按依赖扩大。

## 范围与不变量

允许生产 `engine/src/scientific_reading/reading_pipeline.py`、`engine/src/scientific_reading/mineru_service.py` 中与同源未完成 parse checkpoint 判定直接相关的最小修改；允许新增 `engine/tests/test_v02_parse_recovery.py`，必要补充 `test_reading_pipeline_integrity.py`、`test_mineru_parse_gate.py`、`test_v02_pipeline_contract.py`，不得削弱 004A 的 completed/资产保护断言。新增 `scripts/v02-004b-*`、`scripts/fixtures/v02-004b/`、`docs/codex-v02/V02-004B*`。若为重用原探针必须修改 `scripts/v02-004a-probe.py`，只可兼容旧调用增加证据/环境支持，保留原场景和历史证据。

不改库 schema、持久格式版本、版本号、依赖、锁文件、worker/store/CLI/routes/UI、取消协议或阶段顺序。可在现有 checkpoint 的 result 内添加可选来源信息，但旧 running 记录仍须被安全识别；不能只让新任务可恢复而保留升级前的失败记录无法续接。其他所需生产改动超出本卡时保存证据交回。

严格保留 source PDF 全 SHA、metadata 身份、generation 归属与路径校验、scope/publication_guard、require_stage 的 completed 要求。显式 source 不一致、completed 缺少或不符来源、metadata/目录错配、确认产物损坏均不得被新的例外放行。同源尚未提交的解析可重试；正式缓存仍需验证。不回滚已保存译文或发布指针，不静默自动重放失败任务。

## 验收与交付

1. 原样先跑 004A 的 parse-kill-before，保存失败基线；实现后同一真实进程场景通过，parent ID 不变、旧 PID/claim 正确回收、未提交输出可合理重试，最终资产有效。同时验证旧无 source_sha 的 running checkpoint。
2. 针对上述冲突/完整性负例补必要测试，明确安全恢复所依赖的证据。检查 source 变化不能复用旧 generation，require_stage 和已确认 parse cache 语义不变。
3. 跑 004A 新增九项、pipeline integrity、MinerU parse/resume/artifacts、worker full-read、scope/data guard、generation manifest、candidate rebuild 相关定向回归；Node full-read-pipeline/reading-routes 与构建按实际影响验证。保留实际命令/计数/skip，诊断之外的新失败不得隐藏。
4. 从本工作树测试新建以及基线留下的同源中断结构，不使用用户库或前任务实例。用独立 .venv 与固定依赖、本地合成 provider，禁止真实模型/MinerU/凭据。隐藏启动子进程、记录身份、清理本任务进程。
5. 交回完整提交、最小差异/接口说明、每项证据、未验证项、回退条件，Git clean、receipt review。源码通过不等于安装候选通过；总控后续独立制作新制品复验，因此本卡不打包升级版本或写 B pins。

并行的 004C 仅在 B 做取消合同诊断，不依赖你的未验收修改。不得写对方目录、总控台账、创建任务、合并 main、发布/tag、升级用户实例。
