# V02-004D：恢复修复的安装候选复验

目标：将 004B 的源码恢复结论转为同一可追溯制品的安装证据，验证新旧中断检查点均能在固定运行时安全续接，保持原 Excel/Reader 保护。不是最终 v0.2 发布，也不解决取消/完整实例恢复。

## 工作树与输入

- A `C:/Users/15694/Documents/ChatGPT/deep-literature-engine-v02-004d`，分支 `codex/v02-004d-installed-recovery`，base `bf09c557bcedbba1d843579401d73b7beb65705d`。
- B `C:/Users/15694/Documents/ChatGPT/deep-literature-v02-004d`，同名分支，base `d78e48bbad317c92770b767390addd589b475038`。
- 总控资料 B `C:/Users/15694/Documents/ChatGPT/deep-literature-for-codex`，contextCommit 由派发给出，用 git show 读本卡、AGENTS、session-protocol、004BC-control-review、003D 制品复验报告。开工核对双树身份/clean，B 写 `docs/project/evidence/V02-004D-receipt.json`。应用默认旧 dsh reader 不使用。
- 只读 A 004A 探针/004B 修复与负例报告；只读 dev.2 实物来源证据，不修改其目录或运行实例。并行 004E 的未验收逻辑不加入本包。

## 允许变化

A 仅 package.json/package-lock.json 自身 npm 版本 `0.2.0-dev.3`、engine/pyproject.toml Python 版本 `0.2.0.dev2`；专属 `scripts/v02-004d-*`、`scripts/fixtures/v02-004d/`、`docs/codex-v02/V02-004D*`。源码算法、既有测试、锁依赖不可改；发现问题交回。

B 仅 package.json/package-lock.json 自身版本、foundation/constants.mjs VERSION 为 dev.3；runtime/pins.json 的 plugin/channel、runtime/package-lock.json 本地插件条目、catalog 的 engine 模块版本及 externalEngine 三身份字段；docs/modules/engine.md、changes/engine.md 追溯；`scripts/acceptance-v02-004d*`、`scripts/fixtures/v02-004d/`、`docs/project/evidence/V02-004D*`。不改 workflow/bridge、校验器、其他模块/运行时/依赖/schema，不复用旧版本冒充新 Python 修复。

## 验收

1. 独立环境固定依赖，A 完整 Python 回归、默认 Node offline/assets、正常 build/pack 全部保留实际结果/skip。提交源码后从 clean tracked 源构建 npm/wheel，逐文件证明 wheel 对应当前 Python、客户端对应当前构建。报告提交与源码提交分开。
2. B 同步所有关联身份，模块 check/impact/受影响调用方测试；提交来源后按原校验器打包，验证 ZIP/manifest/解包与安装字节。独立短路径合成实例，由已验证解包安装器安装，不装个人 skill；运行时固定 Node 22.22.2/Python 3.11.16/DSH rc.7/schema 4。
3. **真正使用安装 Python/wheel** 跑 004A 六个进程场景及004B恢复正反例的必要安装版探针。不得用 checkout PYTHONPATH 或脚本 sys.path 覆盖 scientific_reading/reader；先记录解释器/module路径/版本/逐文件 hash，安装调用优先 `-I`，夹具导入和故障注入要明确独立于生产模块。外部 provider 可替身，default runner、真实服务、持久状态、发布与 worker 必须来自安装制品。
4. 旧无 source_sha running checkpoint 必须有真实旧引擎形成的来源证据：可在本卡独立旧候选实例运行已校验 dev.2 再中断，保存原文件/hash，然后经本卡升级或受控迁移到新候选显式恢复。不得改前任务实例或使用用户数据。检查同一 parent、旧PID/claim回收、provider合理重试、已有parse/batch/Reader字节与正式指针保护；不把手工拼JSON单元测试代替全部旧实例证据。
5. 新候选复跑 F1 Excel 原场景（旧书、新DB、Excel-only、双边冲突、legacy、整批保留）、合成主流程 smoke，HTTP/Reader正式资产可读；两次完整宿主重启确认身份、记录、资产与失败需显式续接。客户端未变，不强制重复全部浏览器视觉，但不得从API推断新UI功能通过。F2/取消缺口如实保持。
6. 版本/来源/制品与测试对应，记录清理及未知事项。安装旧checkpoint复验困难时先定位并保留失败，不绕过制品来源或删除 generation 来凑通过。

仅合成论文/本地 provider，无真实模型/MinerU/凭据/用户实例/旧目录写入。全部子进程隐藏启动、身份可核对，停止本卡实例。交付完整 A/B 提交、SHA与测试证据、clean树、receipt review；不发布/tag/合并总控/main/创建下一任务。通过后总控决定004A-F1关闭，但004A-G1与F2不关闭。后续004E若合入，最终组合需另验，不能借用本包结果。
