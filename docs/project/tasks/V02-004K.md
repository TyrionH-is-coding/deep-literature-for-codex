# V02-004K：安装组合中的停止与安全恢复

目标：将已验收的A停止/写前守卫/导出与B工作流组合，形成可追溯隔离安装候选，验证用户通过已有可信操作入口停止、重启保持和恢复同一任务；已有正式资产/记录受保护。风险R2，首次90分钟检查点产出来源/构建证据；两轮修订后复盘方法，不自动降低门槛或扩大范围。

## 固定输入与允许范围

- A `C:/Users/15694/Documents/ChatGPT/deep-literature-engine-v02-004k`，base `b4a9ecc00a76b85c244a0167c79b5a3539e97415`。
- B `C:/Users/15694/Documents/ChatGPT/deep-literature-v02-004k`，base `697dfc4718b7c984223cafc4479b4843fe17dffd`。双分支 `codex/v02-004k-installed-control`；已由总控创建。总控B只读，通过指定contextCommit读取本卡、AGENTS、session-protocol、auto-loop、004J-control-accepted；必要引用004D安装方法、004F/H控制验证与004J脚本，历史结论不能替代新候选证据。
- A仅package.json/package-lock.json自身npm版本到 `0.2.0-dev.4`，engine/pyproject.toml Python版本到 `0.2.0.dev3`；专属scripts/v02-004k-*、scripts/fixtures/v02-004k/、docs/codex-v02/V02-004K*。
- B仅package.json/package-lock.json自身版本、src/modules/foundation/constants.mjs VERSION到dev.4；runtime/pins.json的plugin/channel、runtime/package-lock.json本地插件条目、catalog的engine模块版本及externalEngine身份；inputs/scientific-reading.tgz（遵守已有跟踪策略）、docs/modules/engine.md与changes/engine.md；专属scripts/acceptance-v02-004k*或scripts/v02-004k-*、scripts/fixtures/v02-004k/、docs/project/evidence/V02-004K*。既有打包器/校验器只使用不削弱。
- 不改业务源码、既有测试、Python协议/数据库schema、运行时及依赖版本、用户安装或真实库。真实产品失败在本卡报告给总控分类，只有明确新增授权才改范围；测试夹具问题可本卡修复。

## 一套候选的退出条件

1. 从固定clean source commit正常构建A JS/npm/wheel，再同步B身份并提交固定source，按原打包校验器构建ZIP。固定Node22.22.2/Python3.11.16/DSHrc.7/schema4及精确锁依赖；源码→构建→wheel/tgz→ZIP/manifest→实际安装模块逐文件对应。通过解包安装器安装到本卡短路径独占实例，不安装个人skill。生成文件不手改；构建后报告提交与源码提交分开。
2. 主要集成里程碑跑A build/默认离线与assets链、B完整模块回归；A Python生产字节相对H/I未变，核对后引用595 pass/3 skip固定基线，不再重复源码Python全集。以安装解释器和真实安装模块执行以下必要场景，不能通过checkout PYTHONPATH/sys.path或J目录软链接替换生产来源。默认runner/控制/worker/发布与Handoff/adapter须为安装版本，外部provider可合成。
3. 安装B可信操作入口→真实A worker：持久停止、requested与ack区分、普通retry/resume/attach/类别推进不解停、两次宿主重启不重投、明确恢复同parent。包含同论文两别名依次cancel/resume、停止后新别名、旧恢复键遇新取消和另一论文不受影响。DSH只撤目标队列、独占turn可请求取消、混合turn不误杀；既有源码证据可指导最小场景，不替代安装接口可达与行为结果。用户状态应如实说明独立child不连带停止、当前stage可以完成。
4. 至少一个有真实合成PDF/已提交checkpoint的进行中stage安全停止→确认→恢复场景，不能只验证缺PDFgate：检查源PDF SHA、同parent/generation、已有parse/batch/正式Reader及库指针保持，确认后不调度下一阶段，明确恢复可到合法下一gate/完成。验证一次中断后续接与直接advance非法生命周期写前拒绝（004D-O1）；故障注入和provider替身标清，不用kill代替用户停止。未变化旧格式详细证明引用004D，补验新组合所影响的路径，不重做所有旧夹具。
5. 对新安装候选做个人记录/Excel旧快照保护和最小Reader HTTP资产smoke回归；提供约一页停止→重启→恢复操作样例及实际可访问入口。若只有可信API可用，明确说明，不能从API断言浏览器UI已通过。正式Reader样例/完整主流程演示归005，F2完整三域恢复与真实模型/质量不在本卡伪造完成。

## 交付与恢复

开工核对双base/clean，在B写V02-004K-receipt.json，含实际sessionId、双base/context/path/time/phase。详细日志/构建缓存/实例全在本卡目录或自有短路径；只读复用已验证下载缓存可，不能改旧实例/工作树。先验证路径范围再清理本卡进程/临时物。

报告约一页，索引列精确命令/退出码、源码及制品身份、安装解释器/module路径、SHA、失败/skip/未验证项。大日志归档outputs，不复制历史证据；最终候选与复核所需证据保持可用。交付双提交、clean、receipt review；回退元数据/制品须配套，保留文献资产/控制记录，不能降级消除停止意图。不得发布/tag/合总控/main/升级用户/自行创建后继。004整体及005依赖由总控对新安装证据决定是否解除。

## 总控范围修订（2026-09-21，004K-R1）

安装实测启动返回后8–4452ms的18次可信API请求返回405空体，首个200响应在9880ms完成；不能把响应耗时当成精确路由就绪时间。identity仅按端口先发ready，bridge仍在恢复Handoff，CLI随后解析空响应失败。分类为本任务启动/重启后可操作所必需修复，阻断本卡最终验收，不另开卡。原证据 C:/tmp/v004k/evidence/readiness-1790001190131.json，SHA256 22ede5954abd05f820720dcf057c1e5b09f2e4e1c661bd33525a7c9c23dfbab0；未见资产损坏/误重投，原生浏览器UI报错尚未直接验。

新增仅B允许范围：src/identity.mjs 的启动就绪判断；确有必要时 src/modules/bridge/plugin.mjs 的就绪握手；tests/v02-startup-readiness.test.mjs 及 tests/lifecycle.test.mjs、tests/bridge.test.mjs 的相关有意义回归；application/bridge模块说明与changes、catalog中的相应模块版本及新测试登记。不要改Handoff性能结构、A业务/依赖/schema、其他生命周期逻辑或新增UI。优先最小修改；不要让identity apply等待包含自身的loader.await而自等待。仅端口/identity可用不等于业务ready，必要时核实bridge成功注册事实。若需上述范围外文件，先交总控确认范围。

可继续采集现候选的停止/资产证据，但最终必须固定新B源码并按原打包器生成、安装新B组合。使用新候选补验：首次启动及两次重启返回后立即可信API操作成立，不靠夹具额外就绪等待掩盖错误；初始化失败/未就绪不能谎报running/已启动；保留停止控制、来源和相关资产回归证据。旧源码API等待取证保留；A未变时复用已验证A源码/制品及Python证据，不重复A全集。按实际影响运行B定向与模块检查；最终候选既定回归门槛保留。

## 中断续接判定（2026-09-21，004K-O3）

注册worker崩溃后，普通start/新alias不会自动恢复，原businessStatus可能持续running；这只是残留持久状态，不能作为进程存活证据。原失败和同一parent的后续官方B cancel→acknowledged→resumeStopped成功记录均保留（evidence-final/interruption-1790002721907.json、interruption-1790002912838.json）。同PDF/generation经合理provider重试进入waiting_agent，满足本卡显式中断续接；不要求自动重启，不新增A实现范围。交付提供可信入口的明确两步恢复说明，并将陈旧状态/普通重试提示列已知后续改进；005演示核实此入口可达，不能宣称自动崩溃恢复或仅凭running说明worker存活。该crash样例不替代同候选其他Reader/Excel/已提交资产保护证据。
