# V02-006B：固定 DSH 会话恢复能力取证

状态：ready，依赖 006A 设计和 002 来源审计；父工作域 V02-006。仅完成设计 R1 的可行性取证，不实现产品恢复、门禁或备份 API。

工作树 `C:/Users/15694/Documents/ChatGPT/deep-literature-v02-006b`，分支 `codex/v02-006b-dsh-recovery-probe`，固定 B baseCommit/contextCommit 见台账。DSH 保持 `0.1.0-rc.7`，runtime 锁文件不动。写入只限 `scripts/v02-006b-*`、`scripts/fixtures/v02-006b/`、`docs/project/evidence/V02-006B*`；可在本目录 .local/outputs/inputs 中建立独立未跟踪合成环境。禁止写设计原文、台账、生产代码或依赖/版本文件。

读取 AGENTS、本卡、006A 设计的 R1/身份/禁重放合同、006A 总控复核、001 与 DSH 存储相关证据，按需读固定安装包源码。先验证身份并写 `docs/project/evidence/V02-006B-receipt.json`，含目录、分支、完整基线/contextCommit、时间、phase、可可靠取得的 ownerSession（否则 null）。

## 核实内容

1. 从固定锁与安装包核实原生会话格式、压缩、workspace 注册与索引、会话树、待发队列、工具结果/未完成调用的持久位置；凭据只用合成 canary，不枚举真实用户 DSH_HOME/凭据目录。
2. 优先检查确有定义的导出/导入或离线读写 API，记录包/文件/接口及版本。不能猜 RPC 名称或把创建空会话视为恢复。
3. 在本任务专属源/目标 DSH_HOME 与 workspace，生成含父子关系、工具结果、未完成/待发送状态的最小合成历史。网络模型只指向本地夹具；使用独立自动分配端口，不接管 001 或 003B 进程。
4. 尝试最小离线适配探测到新绝对 root/workspace：保留会话 ID/父子关系、历史顺序与内容，核对宿主重启后实际可见。验证不会自动发送排队请求或重放工具；本地 canary 计数器证明零副作用。无法安全证明时记录阻塞和源码依据，不靠删历史或忽略队列让测试通过。
5. 输出文件/字段白名单、必要映射、排除凭据策略和其自由文本限制；确认哪些结论来自源码、哪些有运行证据。合成 canary 只证明已覆盖字段，不能声称任意历史无秘密。
6. 交付 `docs/project/evidence/V02-006B.md`、可重跑探测脚本与同前缀证据、review 回执、完整提交。若 R1 不可行，明确最小外部依赖/接口改动和阻塞范围；不自行实施 R2-R4 或降级整实例恢复的验收要求。

此卡只审查 DSH rc.7，不依赖 003B 新引擎制品，不覆盖最终三域恢复演练。F2 在总控台账保持开放。只读已核验的共享下载缓存可以，依赖/实例/解压/日志写入必须属于本任务，退出时停止自己创建的进程。
