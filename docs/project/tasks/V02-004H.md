# V02-004H：直接推进的生命周期写前守卫

单目标：修复004G确认的直接advance在后台failed/gate终态时显式执行，会写出自身随后无法inspect的状态。保留worker/CLI拥有后台生命周期，不让advance新增调度或放宽校验。

## 固定输入

A工作树 `C:/Users/15694/Documents/ChatGPT/deep-literature-engine-v02-004h`，分支 `codex/v02-004h-advance-guard`，base `776a018c7855442de38d591517eea831eae5b463`，包含已验收004F和004G。B只读 `C:/Users/15694/Documents/ChatGPT/deep-literature-for-codex`，contextCommit由派发固定。用git show读本卡、AGENTS/session-protocol、004F/004G-control-review；A读004F/004G-report和最小相关源码。默认旧dsh reader不是工作树。核对身份/clean，写 `docs/codex-v02/V02-004H-receipt.json`。

## 冻结行为

- 在已有advance claim内、任何业务/控制记录/runner写入前识别非法显式推进：后台failed、waiting_user、waiting_agent且未由受支持恢复转为queued/running时明确拒绝（建议full_read_pipeline_resume_required），保持所有持久字节。不得强行transition掩盖不变量。检查需在control.stage登记前，避免先改control再拒绝。
- 保留None不自动恢复、completed幂等、后台queued/running正常单步及原source/scope/身份/输出校验。不能根据pipeline.state=failed一概拒绝，worker合法恢复会先改变background状态。
- 已停止普通advance保持004F停止gate，不清意图、不绕过显式resume；resume-stopped和正常CLI start/resume仍恢复同parent。不改004F外部CLI/revision合同或停止状态机，若必须超范围则交总控。

## 允许路径

生产仅 `engine/src/scientific_reading/reading_pipeline.py`。新增 `engine/tests/test_reading_pipeline_lifecycle.py`，最小补相关 `test_reading_pipeline_integrity.py`、`test_reading_control.py` 但不削弱断言；新增 `scripts/v02-004h-*`、`scripts/fixtures/v02-004h/`、`docs/codex-v02/V02-004H*`。不改旧G红灯探针/报告，另建适配当前源码的验证。无版本/依赖/SQLite/schema/TS/UI/算法/B改动。

## 验收

1. 固定base先重现，再证明failed/waiting_user/waiting_agent直接显式调用写前拒绝，业务/控制/资产字节不变、inspect正常；None、completed、queued/running保持合同。
2. 真实模块CLI start→受控provider失败→resume/start→worker到合法gate；不可手动transition冒称CLI通过。采用本卡实际源码并确认导入来源，不误用G私有旧site-packages。
3. 覆盖F停止gate与显式恢复、scope移动/跨paper、同源/变源；相关pipeline/control/worker/scope与原恢复32项回归。完整Python基于固定最终源码运行，可参考F分片但全文件去重覆盖、记录SHA/XML，不在测试中改源码。
4. 自有短路径合成库/环境、无真实模型/凭据/用户安装或旧实例改动。交付commit、错误合同、来源/实际测试/清理/回退记录，Git clean、receipt review。源码通过不代替B接入/安装，不合main/发布/创建后继。
