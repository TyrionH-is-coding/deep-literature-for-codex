# V02-004I：停止控制跨包适配

单目标：将已验收F/H的停止/read-control/显式恢复合同无损接入A TypeScript导出与B engineAdapter，为下一卡workflow取消接入准备稳定接口。当前不改Handoff行为或产品界面，不宣称用户取消链通过。

## 固定双工作树

- B `C:/Users/15694/Documents/ChatGPT/deep-literature-v02-004i`，branch `codex/v02-004i-control-adapter`，base `21e34c9763859442df33d047d12749e9a8b53744`。
- A `C:/Users/15694/Documents/ChatGPT/deep-literature-engine-v02-004i`，branch `codex/v02-004i-control-adapter`，base `2720fac32cf2b989523079405abbd0e4f3698e07`。
- B总控只读 `C:/Users/15694/Documents/ChatGPT/deep-literature-for-codex`，contextCommit由派发固定。git show读本卡/AGENTS/session-protocol/004F与004H-control-review；读bridge模块上下文与A F精确CLI合同。应用默认旧目录不使用。核对两边clean并在B `docs/project/evidence/V02-004I-receipt.json`写身份/base/engineBase/context/time/phase，两边提交单独记录。

## 冻结合同

1. A新增并导出 `engineResumeStoppedFullRead(config, jobId, suppliedInput, {requestId, expectedRevision})`，生成F既有full-read-pipeline-resume命令的--resume-stopped/--request-id/--expected-revision/--input参数，复用当前trustedProviderEnv与withEngineScope链。老engineContinueFullRead签名/行为保留；不得让分类代理工具自动取得解除停止的新能力。stop/read-control可复用已有engineJson，不为简单透传增加多余层。
2. B engineAdapter对显式resume参数走新导出，不能沿用老分支悄悄丢掉控制参数。缺失新导出明确engine_stop_control_unavailable，不回落普通resume。旧start/普通resume/attach保持调用路径。
3. 识别F的reading-control-v1快照。stop/read/显式resume的成功，以及普通resume/attach遇stop的exit2业务gate，都应保留真实request/ack/terminal、revision、parentJobId、businessStatus、pipelineState、independentChildrenStopped等信息。验证返回parent与请求job一致、控制字段基本类型/状态及嵌套身份；非法/错parent/错误envelope拒绝，不把任意ok=false当业务成功。原业务failed/completed不得改成canceled，收到请求不等于停止完成。
4. 明确错误合同：现有错误安全归一化保留；过期revision、ID冲突、未知调度结果、scope拒绝应能被调用方区分。requestId为空/过长、revision非安全非负整数等应在调用前拒绝。既有接口不携带新参数时兼容旧API；不声称旧引擎拥有停止能力。
5. 不改F/H Python协议、分类工具白名单、Host/Origin或scope边界，不新增HTTP操作/UI、不改workflow持久数据/取消/派发语义。下一卡再接入取消意图与引擎事实。

## 允许路径

A仅 `src/cli.ts`、`src/index.ts`；新增 `tests/v02-stop-control-api.mjs`，`package.json#scripts`仅登记新测试到适当默认离线链（不改版本/依赖）；新增 `scripts/v02-004i-*`、`scripts/fixtures/v02-004i/`、`docs/codex-v02/V02-004I*`。不手改lib/client生成产物。

B仅 `src/modules/bridge/services.mjs`、`tests/bridge.test.mjs`及可新增 `tests/v02-control-adapter.test.mjs`；bridge说明/变更记录、catalog仅bridge版本0.1.1与本卡测试归属；新增 `scripts/v02-004i-*`、`scripts/fixtures/v02-004i/`、`docs/project/evidence/V02-004I*`。不改引擎pins/产品版本/锁/用户安装或总控台账。

## 验收

- 参数完整性、零revision、重复请求、请求中/确认/终态、普通resume/attach被阻止、过期与错误返回、丢失导出、伪造快照/parent、scope及trusted provider环境的真实透传，现有兼容测试不削弱。
- 不能仅mock证明跨包能力：在本卡独立构建的A JS导出+B adapter、固定Python3.11.16实际模块中验证新合成parent的read→stop→普通resume阻止→显式resume到合法gate；使用scope验证跨paper/分类拒绝。无真实provider/模型/用户库，可用缺PDFgate及合成数据，不把此源码集成称为安装验收。逐文件/提交证明导入来源。
- A build+相关TS离线回归；B modules check/impact/test bridge以及直接workflow调用方合同。没有Python生产改动不重复整个Python全集，若发现真实接口缺陷超范围则交总控。
- 固定源码后记录精确命令/结果/未验证项，工作树clean，B回执review，双commit与清理/回退说明。不得发布/tag/合main/创建后继或升级已安装实例。构建输出、库、环境全在本卡自有目录。
