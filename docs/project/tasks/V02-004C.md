# V02-004C：核实取消职责并设计最小合同

目标：闭合 004A-G1 的事实与方案，不在本卡实现新取消状态机。确定现有“取消”实际影响哪些宿主队列/会话、handoff 记录及 A detached worker，明确用户可理解的边界、剩余风险和最小后续实施范围。

## 固定基线与阅读

B 工作树 `C:/Users/15694/Documents/ChatGPT/deep-literature-v02-004c`，分支 `codex/v02-004c-cancel-contract`，base `93bbe23c2b35da3ff5073da47fe229f32d7d367a`。上下文从总控 B `C:/Users/15694/Documents/ChatGPT/deep-literature-for-codex` 指定 contextCommit 用 git show 读取本卡、AGENTS、session-protocol、004A-control-review。应用默认旧 dsh reader 不使用。

A 只读 `dcdb8e6cb23dd8329ceabd4487db0a79fae470ea`，从 `C:/Users/15694/Documents/ChatGPT/deep-literature-engine` 用 git show 读取 004A 报告和必要 worker/store/CLI 合同，不随该目录 HEAD 更新改变输入；不读取或修改 004B 工作树。开工核对 B 状态/完整 HEAD，写 `docs/project/evidence/V02-004C-receipt.json`。

先 `npm run modules -- list/context workflow`，读 workflow 公共入口/handoff 和取消注入的 bridge 服务/测试，按实际调用链补上下文，不加载全仓库历史。

## 写入范围与产物

只新增 `docs/project/design/cancellation-contract.md`、`docs/project/evidence/V02-004C*`、`scripts/v02-004c-*`、`scripts/fixtures/v02-004c/`、`tests/v02-cancel-contract.test.mjs`。不改生产、版本、锁、catalog、既有断言或总控台账。需要运行时则仅固定 DSH rc.7、独立短路径合成实例/随机端口、合成文本，不用真实模型/凭据/用户库；只清理本任务实例与登记进程。

1. 用代码与实际有界探针明确 submit/dispatch/cancel/operate 的状态及拥有者；覆盖尚未投递、已排队、当前 turn 包含目标任务/其他任务、引擎已独立运行、cancel 后重启与显式 resume。能通过现有注入测试证明的边界与真实宿主才可证明的边界分别记录。引用旧结果不能冒充新执行。
2. 明确取消请求是否抑制后续自动 dispatch、引擎是否继续产生资产、对其他任务/共享 turn 有无影响；不得把杀测试进程视作受支持用户取消。无受支持入口时记录缺口，不私造 API 来获得通过。
3. 设计最小合同：推荐用户动作应停止哪个域、何时算已取消或请求中、是否允许已提交资产保留、后续是否必须显式恢复、旧任务兼容性与重复请求/重启行为。区分“取消后续调度”与“立即终止运行步骤”的成本，推荐范围应解决稳定性需求，不追加产品新功能。若需要新的 A/B 接口，给出具体建议和负例/故障点，供总控另开实施卡，不自行实现。
4. 执行相关既有 handoff/bridge 取消合同和新增必要测试；若不能做真实宿主取消，清楚留下验收缺口，不把纯 mock 计为端到端通过。保留脚本、命令、来源、退出码、实际测试数与进程清理。

交付固定提交、报告、设计、未验证项与建议下一单目标卡；receipt review、Git clean。诊断交付通过不等于取消已实现或 004 已通过。不能合并 main、发布、升级用户安装或创建后继任务。

与 004B 并行依据：本任务写 B 专属文档/探针，A 输入固定且只读；004B 只改 A 同源 generation 恢复，不改变取消/宿主接口。实例、构建缓存和输出均独立。任何新增交叉依赖先交总控收敛范围。
