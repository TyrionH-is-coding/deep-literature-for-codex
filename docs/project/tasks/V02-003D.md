# V02-003D：在文献详情中显示 Excel 同步冲突

目标：修复 003C 已证实的 F4，让用户看见同步为何暂停、哪篇文献哪些个人字段冲突及如何安全处理，并以新候选安装验证。保持已通过的 F1 数据保护。用户已授权路线内最小修复，不需重复确认。

## 固定上下文与工作树

- B：`C:/Users/15694/Documents/ChatGPT/deep-literature-v02-003d`，分支 `codex/v02-003d-conflict-feedback`，base `c7757c49d509bcec28464120a82cd87d7360458f`。
- A：`C:/Users/15694/Documents/ChatGPT/deep-literature-engine-v02-003d`，分支 `codex/v02-003d-conflict-feedback`，base `e3f58d1710413b5c6340fe55974a227b97ee8373`。
- contextCommit 见总控台账；任务卡及 003C-control-review 从 `C:/Users/15694/Documents/ChatGPT/deep-literature-for-codex` 的该固定提交读取，新工作树旧台账不替代本次派发。
- 读取 B AGENTS、本卡、003C 总控复核/原报告、A client/client.js 的详情区与现有相关测试，按需读 routes 传入合同。写 B `docs/project/evidence/V02-003D-receipt.json`，记录双仓库完整基线/路径/分支/contextCommit/时间/phase，ownerSession 无可靠真实 ID 则 null。

## 已确定的最小行为

003C 的 `ui-paper-api.json` 提供真实形状：paper 详情的 `job.status=waiting_user`，`job.detail.reason_code=xlsx_user_fields_conflict`，`job.detail.required_input.conflict_details` 列出 paper_id/field/code，error.detail 表示整批未写。现有抽屉仅恢复 PDF gate，未显示 Excel 冲突。

1. 在文献详情显示可读的“Excel 同步已暂停”及对应原因，明确原工作簿与数据库均保留、整批未导入。三个字段使用现有中文名称（个人思考、个人理解程度、用户笔记）；显示可定位的文献 ID。若当前文献因同批其他文献冲突暂停，要正确说明，不能把别人的冲突字段误归给当前文献。
2. 提示先备份 Excel、核对双方记录并处理差异后保存关闭再重试；可复用现有定位 Excel 按钮，不添加自动取舍、强制覆盖、自动继续或冲突编辑器。不输出个人字段内容或凭据；不把内部调试堆栈塞给用户。
3. 同一提示处理已知身份/基线异常的现有 xlsx pending reason，缺少可选 detail 或未知 reason 使用保守通用说明，不能渲染崩溃或假称已解决。正常成功、PDF waiting_user 的既有提示/操作保持原样；重新加载/关闭重开详情后按持久 API 状态渲染。
4. 使用 textContent/安全 DOM 生成提示，恶意字段、paper_id 或 reason 字符串不能执行 HTML/脚本。不要用全量 required_input JSON 当用户提示。

## 写入范围

A 生产仅 `client/client.js` 及其由原构建器生成的 `lib/client.js`；允许 `tests/client-ui-contract.mjs`、`tests/client-actions.mjs`、新增 `tests/client-xlsx-conflicts.mjs` 和 package.json 的测试 scripts。版本元数据允许 package.json/package-lock.json 仅自身 npm 版本改为 `0.2.0-dev.2`；现有 Python wheel 分模块版本保持 `0.2.0.dev1`，Python 源码/算法/依赖/schema 不变。允许 `scripts/v02-003d-*`、`docs/codex-v02/V02-003D*`。不改 routes、worker 或通用作业协议；确有缺失接口才保存证据交回，不擅自扩大。

B 可改 package.json/package-lock.json 仅自身版本、foundation/constants.mjs 仅 VERSION，均为 `0.2.0-dev.2`；runtime/pins.json 仅 plugin/channel，runtime/package-lock.json 仅本地插件条目；catalog 四个 engine 身份字段；docs/modules/engine.md 与 changes/engine.md 的制品追溯信息；新增 `scripts/acceptance-v02-003d*`、`scripts/fixtures/v02-003d/`、`docs/project/evidence/V02-003D*`。不得改其他运行时/依赖/生产逻辑/校验器/既有失败证据/总控台账。自有 inputs/outputs/.local 保存制品和夹具，禁止写上一任务实例或用户安装。

## 验收和制品门槛

- 先用真实 003C HTTP 形状复现现有缺口；新增实际 DOM/渲染断言（仅正则出现字符串不够），覆盖正常状态、三个字段、缺失/未知 detail、同批其他文献、PDF gate、刷新重开与恶意文本。原有保护断言不放宽。
- A 使用 002 固定依赖、独立构建环境；运行新定向测试、既有相关 UI/action/build 检查及默认 Node 离线/资产链，把新测试接入默认链。Python 无差异不要求再次重复 515 项；wheel 构建/安装与来源对应仍要验证。提交 A 来源后打包并记录 npm dev.2 / Python dev1 的准确版本组合，绝不复用 dev.1 包名冒充新客户端。
- B 同步完整版本/锁/pins/catalog，运行模块检查及受影响调用方测试，先提交产品来源再从干净 tracked 源打包验证解包。新短路径合成实例由解包程序安装，不装个人 skill，保持固定 Node/Python/DSH pins，记录制品和安装字节来源。
- 在新安装的真实浏览器文献详情复现冲突，检查可见中文原因、文献/字段定位、整批保留说明；刷新/重开与两次宿主重启后提示准确。不要把 API 正确计为 UI 正确；保留观察/截图或 AX/DOM 证据及地址/实例/时间。确认正常文献/PDF gate/Reader 操作没有回归，固定候选再跑 F1 旧书/Excel-only/双改/legacy/整批保留的安装断言，复用 003C 合成 smoke 意图并写专属结果。
- 业务调用仅本地夹具；Office 实际打开不是本卡强制门槛，准确保留未测试限制。F2 仍开放，不能将库恢复视为整实例恢复。
- 交付 A/B 完整提交、来源/制品摘要、准确测试计数、已知限制、停止自身进程证据与 review 回执。禁止合并总控/main、发布/tag 或操作用户数据。新真实问题按证据交回，不无限扩展功能。
