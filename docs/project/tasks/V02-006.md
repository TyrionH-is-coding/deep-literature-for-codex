# V02-006：三域备份恢复到独立新实例

目标：通过可信CLI将受支持实例停机备份，恢复到同平台、同完整制品的独立新根；库/资产、原生会话、handoff关系完整可读，两次重启不自动推进；明确核对后可继续原任务，重复请求不新建parent、不重复投递、不覆盖正式资产。一卡包含必要实现、接入、打包和隔离安装演练。风险R2，F2只在新组合实际通过后关闭。

## 固定输入与边界

- B工作树 `C:/Users/15694/Documents/ChatGPT/deep-literature-v02-006`，分支 `codex/v02-006-instance-recovery`，base `0b512ab25955e9f7dd3ddae3f15e88c14d0e03c1`。
- A工作树 `C:/Users/15694/Documents/ChatGPT/deep-literature-engine-v02-006`，同名分支，base `ad00bc5a84110346801dbb6e98c8b9e19135287d`。contextCommit由派发消息固定，来自只读总控B，不合并总控文档分支。
- 先读当前PROJECT、本卡、AGENTS、session-protocol、v0.2-plan，目标模块context和公共入口。复用 `docs/project/design/instance-recovery.md` 的停机/新根方案、006B固定DSH导入取证及总控复核；其旧身份/停止描述必须按本卡修订。005演示可读，不重新载入全部历史。
- 只操作本任务新建的 `C:/tmp/v006/` 源/目标/构建/输出及双工作树。用005代表性合成数据合同建立本任务源，补入真实产品会话，不对005/004K或用户实例写维护标记、冻结、备份、停止或迁移。原始制品/下载缓存可核验后只读复制。旧dsh reader不可开发。
- 首版限定同OS/架构、同完整候选、固定DSH rc.7、单workspace和明确支持的preset/状态类型。未知媒体/spill/插件状态/多workspace等在写目标前明确拒绝并列原因，不遗漏后声称“完整”。不得做在线增量、原位覆盖、跨版本/跨平台迁移、新模型路由或新UI体系。

## 首个检查点：先确定恢复身份和门禁合同

开工写B `docs/project/evidence/V02-006-receipt.json`（真实sessionId、双base/context、路径、时间、phase）。首个90分钟内用最小合成源/新根取证下列静态风险并交一页接口合同，向总控报关键取舍，不先扩大实现：

1. 当前parent哈希包含request.payload.data_root；旧library restore改根却保留jobId，可能触发 full_read_parent_mismatch。先复现，再用严格、可审计的恢复身份映射/原身份记录解决；保留parent/generation与幂等事实。禁止全局放宽stable_job_id检查，非恢复作业校验不得降级。
2. 新instanceId还影响job.status.scope及reading-control-v1。验证分类/session/paper关系后显式重绑定；保留revision、requestId、operations、coveredStops等停止/恢复事实，只在受控恢复中清理不能移植的worker/PID/activeStage等执行身份并记录每项变换。不能清空control以获得可续跑。
3. Handoff.open/_refresh会对账并可能补发stop；安装activateRelease会启动，maintenance路径会跳过部分启动检查。恢复门禁必须早于这些副作用，普通与维护启动均不能绕过；`/ready`不等于恢复事务完成。
4. 库备份的冻结会在单次调用结束释放；三域复制需同一持续冻结。不能外套锁再递归调用造成自等待，也不能用acknowledged代替所有独立写者已停止。

这不是单独新任务。确定的范围内实现可继续；本卡允许新增整实例manifest、恢复回执和受控身份映射记录。需要先给最小提案的是改变现有schema/合同、全局身份算法或扩大兼容范围，不因必要新回执而暂停。每次检查点记录下一步及预期证据；两轮同类返工无新证据时复盘，不无限增加任务编号。

## 初始允许路径（只限上述恢复职责）

- B恢复：`src/modules/releases/instance-recovery.mjs`、`dsh-recovery.mjs`（可新增）、`index.mjs`、`library-transfer.mjs`、`releases.mjs`。
- B接入：`src/cli.mjs`、`src/install.mjs`；`src/modules/lifecycle/control.mjs`、`supervisor.mjs`、`index.mjs`；`src/modules/workflow/handoff.mjs`、`src/modules/bridge/plugin.mjs`。只增加恢复事务/无自动启动/验证模式/持久投递门禁，不重写普通调度或取消。
- 共享门禁状态若必须跨模块读取，可新增 `src/modules/foundation/recovery-state.mjs` 并改其 `index.mjs`，由releases拥有事务写入；遵守公共入口，禁止引入lifecycle→releases的循环依赖。
- A：`engine/src/scientific_reading/library_backup.py`、`data_guard.py`、`__main__.py`；可新增专用 `recovery_identity.py`；`background_store.py`、`reading_control.py`、`reading_pipeline.py`、`worker.py`仅恢复身份校验/受控继续接点。不能全局改job哈希/调度算法、原Reader/Excel业务；若确需其他文件，先报最小依据由总控修订范围。
- 对应有意义的B `tests/*recovery*.test.mjs` 及现有releases/install/lifecycle/bridge/reading-control相关定向段；A `engine/tests/*recovery*.py`、`test_library_backup.py`、`test_data_guard*.py`、`test_reading_control*.py`、`test_reading_pipeline*.py`、`test_background_launcher.py`、`test_worker_full_read.py`的相关段。
- 专属B `scripts/acceptance-v02-006*`、`scripts/v02-006-*`、`scripts/fixtures/v02-006/`、`docs/project/evidence/V02-006*`；A `scripts/v02-006-*`、`scripts/fixtures/v02-006/`、`docs/codex-v02/V02-006*`。大日志和实例放本任务outputs/短路径，交索引，不复制全部历史。
- 必要模块登记/说明与changes只限foundation、lifecycle、releases、workflow、bridge、application、engine及相关tooling测试登记。打包器仅使用；A npm自身版本/lock及Python版本、B自身版本/constants、engine pins/runtime lock本地插件条目、catalog engine身份和制品摘要可配套到内部 npm `0.2.0-dev.5` / Python `0.2.0.dev4`。Node22.22.2/Python3.11.16/DSHrc.7/schema4与其他依赖不升级；生成制品不手改。

## 实现与退出条件

2026-09-22 总控范围澄清：已授权Python `0.2.0.dev4` 的版本同步包含 `engine/src/scientific_reading/__init__.py` 的 `__version__` 常量，与 `engine/pyproject.toml` 一致；仅此版本元数据，不扩展运行时、schema或依赖范围。

1. 三域包有版本绑定、三域齐备/明确空状态、路径/大小/重复项预算、每项摘要、关系和变换记录。DSH离线适配严格绑定已取证格式并保留原历史前缀、sessionId和父子关系；未知必要状态拒绝。凭据存储/profile不导出；已知结构化秘密canary拒绝，不能声称自动识别任意自由文本秘密。
2. 源获得维护互斥并关闭入口，正常停止宿主、核对独立worker/写者边界，持有覆盖三域复制的引擎冻结，成功后不覆盖式发布备份。失败/超时不出成功包，持久中断标记防止未知状态自动启动；不按陈旧PID杀进程。
3. 目标只能是全新独立根，拒绝源/祖先/后代/别名/junction/非空有效实例。先建立恢复所有权和持久门禁，再安装/导入；半恢复不能普通启动，源及旁边有效实例不变。同制品预检严格；ready前全部关系、来源路径与资产读回有效。
4. 完整产品组合的恢复验证模式允许历史、库及Reader读回，持续拒绝模型prompt/原生队列/工具/worker及handoff自动推进，不靠缺密钥或2秒观察窗。保留未决prepared/uncertain/cancel事实，未知投递不能自动重发。验证后仍有明确待核对门禁；显式确认/继续采用已有停止恢复的revision/幂等合同，有审计回执，不能启动任意旧队列。
5. 关键验收：中文/空格新根的三域往返与两次重启；完成文献/Excel个人记录/正式Reader保持，产品会话历史及分类绑定可读；进行中/停止代/未决投递不自动重放；同parent显式继续和重复/丢回执不重复执行。禁重放应有模型/工具/worker计数与显式继续阳性对照。
6. 失败验收覆盖缺域、摘要/格式/版本错误、路径越界/别名与非空目标；库已恢复后DSH失败、ready前中断；活跃写者/并发普通及维护启动、备份互斥；凭据canary。分别断言源与另一有效实例不变、目标隔离；允许以等价分支组合减少重复，但每项要求给证据映射。不支持的状态先拒绝而非破坏性猜测恢复。
7. 固定clean源码后按原工具构建新A/npm/wheel+B ZIP，在本任务源/目标真实安装验证。新功能不能沿用dev.4安装通过结论；定向/相关调用方按R2执行，最终主要里程碑做受影响A完整回归及B全模块，记录失败/skip和可复用证据，不重复未变化源码全集。新模块登记/边界必须modules check。

## 交付与停止条件

约一页报告加证据索引：一个备份→新根恢复→两次重启→明确继续的可观察演示，精确命令/双source与交付提交/新制品SHA、范围与拒绝类型、真实验证、未解决项及数据回退条件。必要原始证据保留；对大材料记录本机路径/哈希，最终候选复核材料随候选归档。收尾停止只归属本任务的进程、恢复夹具配置、双工作树clean、receipt=review。

不改用户真实安装/库/凭据，不发布/tag/合总控/main，不自行建后继。真实登录/科学质量留007；本卡用隔离合成输入不需要用户账户。F2是否关闭由总控对新组合三域证据决定，不能只交备份ZIP或源码测试就宣布解决。恢复失败不覆盖有效数据；重试用另一个新目标，已有ready目标只核对不覆盖。
