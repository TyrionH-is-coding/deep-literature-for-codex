# V02-007：一篇真实论文的主流程使用验收

目标：同一 dev.5 候选在新隔离实例处理一篇真实论文，用户能阅读并核对内容、编辑记录，重启后读回。风险 R2。状态 ready / 等待用户论文、真实服务条件与人工反馈；尚未创建开发任务或执行真实模型调用。不得把准备完成写成使用通过。

## 固定输入与允许范围

- 依赖 [006 总控验收](../evidence/V02-006-control-review.md)。A 集成 `d55d4e91893945c94b6f61c7fcc138a932933b8d`、B 集成 `a81aa94d0dce98d1e97b660e899c36a2300def3d`；生产源 A `8b195c9109efd92ba74c37bb244b2c6b155060de` / B `058afb0fe8ebd5e39a4f9ca7e495011e4cde61ca`。
- 只用 `C:/tmp/v006/candidate-r4/deep-literature-for-codex-0.2.0-dev.5-win-x64.zip`，SHA256 `cb0d5a01b3cad6c6292aa10a7eaf1b9af76c244a069e856322fc7561d6c35044`。不重建制品、不拼接模块或升级依赖。
- 执行前由总控登记唯一 session、精确 base/context、独立工作树与端口。预留 `C:/tmp/v007/` 仅作本任务新实例/输出；若已存在，先查归属，不覆盖。只允许本卡专属验收脚本/报告/脱敏索引及新实例写入。用户安装、库、006/005 实例、已有凭据保持原状；不读取或复制既有凭据，不改用户 Skill。
- 首个工作量检查点为开始执行后 90 分钟。初始无生产代码修改权限；发现原目标必需修复先由总控记录最小范围和新候选身份，再验受影响部分，不自动新增功能。

## 用户参与的具体缺口

1. 提供熟悉领域的一篇真实论文 DOI/公开全文链接，或授权按指定领域选择一篇；优先可合法公开取得的全文。
2. 明确本次解析/模型使用范围或额度上限。建议一篇、最多一次额外重试；未获真实服务使用条件前不发付费请求。
3. 新隔离实例备好后，用户本人完成工作台模型登录/配置、模型选择及 MinerU Key 保存。外层 Codex 登录不等于工作台授权；密钥不经聊天、脚本参数、日志或报告采集。
4. 阅读最终样本并给出“可用 / 需修正 / 不可用”及具体理由，实际编辑一条个人记录。人工反馈缺失不能标为通过。

## 最小验收步骤

1. 核验 ZIP、安装来源与全新实例身份，通过当前用户可达入口入库、取合法 PDF；记录题录与 PDF SHA，不能以网页充正文。
2. 真实 MinerU 解析，使用实际选择的模型翻译/导读，形成正式 Reader。记录模型/服务名称、task/parent、终态、用量（能读取时）、干预与重试次数；不可用数据写未知。凭据只在本机授权配置中保存，证据脱敏。
3. 用户对照原文检查研究问题、主要结论、关键数值/单位、图表对应和局限说明；总控记录具体定位与判断。合成样例不替代本步骤。
4. 用户通过现有记录入口编辑保存；正常重启一次，读回 Reader 与个人记录。若使用桌面 Excel，记录实际成功或缺口；不把打开总表回退反馈等同自动定位行。
5. 若真实服务实际失败，保留失败并从现有入口恢复同 parent；未发生则不声称真实服务故障恢复已验，不主动制造额外付费失败。结束后停止本任务实例，保留证据。

直接复用同候选的 006 来源、完整回归、三域恢复、门禁、revision/幂等、派生中断及资产保护证据，不重复无变化源码全集或全套迁移。候选变化时重新绑定来源并补受影响验证。

## 退出与交接

退出需要：真实论文处理完整、Reader 内容有原文依据、记录重启保持、用户明确认可该样本，无数据损坏/越界/主流程阻断/误报终态或影响结论的内容错误。服务或用户输入缺失时只暂停受影响链；科学结论不可由脚本自动代判。

交回一页结论及脱敏证据索引，记录固定制品、实例、论文、模型/解析服务组合、用户反馈、失败/重试和未验项。通过仅代表本次 Windows/服务/样本组合，其他平台和正式发布由 008 单独收口；不自行合 main 或发布。
