# 自动总控循环

用户已授权总控自动启动 002、验收通过后启动 003，并允许互不影响的任务并行。当前模式为本总控任务的 heartbeat，每 10 分钟检查一次；自动化编号由 tasks.json 记录。

## 恢复入口与权限范围

总控仓库：`C:/Users/15694/Documents/ChatGPT/deep-literature-for-codex`，分支 `codex/v02-control`。引擎仓库：`C:/Users/15694/Documents/ChatGPT/deep-literature-engine`。旧 `dsh reader` 目录仅是应用登记的项目入口，不是验收基线，保留其用户修改。

每轮先读 PROJECT.md、tasks.json、本文件及当前活动任务卡；只在需要审查时读对应报告、代码与证据。历史聊天和旧 v0.2 计划不替代当前仓库事实。持续授权涵盖路线内任务创建、测试、复核、最小修复、分支集成和进度维护；不自动恢复被排除的新功能。产品安装与用户文献库不作为测试环境。

## 每轮步骤

1. 核对总控 Git 状态与台账；保留任何用户修改。先处理已有任务，检查已登记的真实 session（优先 wait_threads 紧凑快照），再检查它的固定 worktree、任务回执、提交和报告。列表找不到 session 不代表任务不存在；不得因为 clientThreadId 未解析就重复创建。
2. kind=task 且 reserved/queued/in_progress/review 中尚未终止的开发任务占用并行名额；workstream 的汇总状态不重复占名额。派发前写入唯一任务编号、固定 baseCommit/contextCommit、分支、worktree、writePaths、依赖与 dispatchState=reserved；创建后保存返回 ID，并等待一次进展。真实 ID 尚不可用时只保存 clientThreadId，不把它传给需要 threadId 的工具。
3. 开工/交付回执只提供状态线索，不能代替验收。结束时读当前提交的差异、报告与原始结果，检查是否越界，补做必要的合同/调用方检查。测试退出成功、聊天说完成或有一个报告文件，都不单独构成通过。
4. 满足任务目标且证据充分后串行集成到指定分支，记录 integrationCommit 与遗留问题，再解除对应依赖。只诊断任务可接收已归因问题，但必须明确它是诊断交付通过，而非产品全通过。002 必须完成源码/制品来源审计、接口地图及实际测试基线；阻断这些目标的失败未解决时，不开 003。
5. 任务失败时保持后继依赖未就绪，优先向同一开发任务发送具体返工要求。需要产品修复且超出原卡时，总控创建单目标修复卡，避免把修复混入基线审计。相同阻塞连续两轮没有新证据时记录 blocked、原因与可行动请求，停止无效重试；其他独立任务可以继续。
6. 后继条目若是 workstream/milestone，先拆成单目标任务卡再派发。002 通过后先拆 003 的 F1 冲突合同/最小修复，记录行为选择与回归场景；不要把整个存储重构一次交给一个开发任务。
7. 更新 PROJECT、台账、决策和证据，提交到总控分支。开发任务各自提交本工作树，不能并行写总控台账。状态不变时保持安静，仅在实质变化、完成、失败或需要用户参与时通知。

## 并行判定

最多两个开发任务并行；只有前置证据齐备、writePaths 不重叠、没有尚未确定的共享接口改动，且测试不争用同一实例/数据库/端口/构建输出时才并行。独立 worktree 是必要条件，不是充分条件。无法证明独立性就串行，集成始终串行，合入后验证两者组合。

004D/004E已结束并串行集成，恢复F1已关闭。004G诊断已接收，总控独立复现内部直接advance缺陷；004F/H源码已验收，当前004I只冻结A导出/B adapter控制接口；workflow取消行为等适配通过后再做，未冻结接口前不为填满并行名额提前实施；不能绕过004整体门槛进入005。最终含停止控制和B接入的组合需重新安装验收，不复用dev.3局部结果冒充全通过。

## 结束和暂停

循环以当前 v0.2 路线的验收目标为边界，完成候选交付后暂停自身并汇报，不无限增加范围。需要用户本人登录、实际使用验收或无法从证据判断的产品取舍时，暂停受影响链并说明具体缺口；不伪造用户验收。用户要求停止或调整时立即以新指令为准。自动化不等于计算机关闭后仍可在本机运行。
