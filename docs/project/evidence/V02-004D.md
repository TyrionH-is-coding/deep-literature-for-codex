# V02-004D：恢复修复的安装候选复验

phase=review，待总控独立复核，未发布。固定context 11581f7f2b0530a11179f0b25b90de4cb6559f1d，A base bf09c557bcedbba1d843579401d73b7beb65705d，B base d78e48bbad317c92770b767390addd589b475038。双树路径、分支、初始clean和时间见receipt；ownerSession无法可靠取得，保留null。

## 范围与制品

A仅npm dev.3、Python dev2身份及专属探针/证据；B仅dev.3身份、pins/本地插件lock integrity/catalog engine字段、追溯与专属探针/证据。算法、原测试、运行时/依赖、校验器、workflow/bridge、schema未变，scope-audit.json验证了排除路径及JSON字段差异。004E未加入，取消与F2不解决。

- A制品源码：a45aff9a6d5419c1b7a5cbc7954355176d56fac5。
- B clean打包源码：c78dc5846dac799b024a4cd07dfbef40bc2e8f6a。
- tgz SHA256：a2e9e629140688df3fce947c4118e32157326a7e0a75c3d7952f6a67b413ba44。
- wheel SHA256：37f3da559ba74f022f4b73c3757b55299254ee50a75f04af49162b00749f4f83。
- ZIP SHA256：74bc15abc25499ecec9d8971e585e2c46dd4ce9ac3410aea86dd12b8bd6f0a8a。

原package-release校验器验证434清单文件＋BUILD-MANIFEST共435文件。安装使用verified-extraction/install.ps1，未传InstallSkill。独立新装v4d、独立旧装再升级v4do都exit0，固定Node22.22.2/Python3.11.16/DSH rc.7/schema4。新安装app的41个src文件与清单一致；两实例51个installed Python文件逐个匹配新wheel/A源码，npm版本及client SHA匹配本次构建。详见artifact.json、package.txt、installed-app.json、installed-provenance.json、upgraded-provenance.json。制品冻结后新增/修订的只是验收驱动和报告，不把最终证据提交冒称打包提交。

## 已执行验收

A完整Python 547 pass/3 skip（Windows symlink权限、Unix zombie、未启用原生凭据服务），默认Node offline42条、assets4条、正常build/pack通过。独立构建环境版本和二进制匹配固定归档。源码测试提交deec00a与制品a45aff9仅专属探针差异，生产/原测试/依赖无差异；A证据保留完整命令和结果。

B modules check 10模块/47源文件/152导入，impact unowned为空；engine调用方55 pass/1 Windows POSIX skip，foundation及调用方78 pass/1 skip。末轮check/impact再次通过，未为纯探针新增无关测试。

安装版32项断言全通过（23恢复正反例＋004A九项），六真实进程场景全部completed，同parent、schema4、PID/claim回收、解析提交前provider重试2次/其余1次、已保存解析/translation batch不重写、不完整新代不替换正式Reader字节/路径。source_sha、身份、缺PDF、正式目录/manifest、require_stage、确认缓存损坏等负例重复拒绝。取消边界断言只确认当前缺口，不是取消验收。完整结果在A docs/codex-v02/V02-004D-evidence/installed/及installed-summary.json。

所有安装探针均使用安装venv的 -I -X utf8；每个探针worker保存解释器、模块位置、版本、sys.path和所有Python文件SHA。仅合成provider/翻译、显式故障边界和标准库测试断言适配器是夹具，默认runner、真实服务、持久状态、worker、derived worker与发布来自安装wheel。没有checkout PYTHONPATH/sys.path覆盖生产模块，也未向安装venv加测试依赖。原源码offline中已有PYTHONPATH测试不冒充安装证据。

真实旧dev.2 ZIP SHA a96a0d6bd702e7edd3f9e5327555c978aded3e189ceb06948a9d9da73fcdaca9重新校验并在本卡v4do安装；旧wheel dev1的真实worker被中断产生无SHA running记录。旧引擎后续复现generation_workspace_conflict；经新候选安装器升级，在原parent/原checkpoint上显式续接completed。首轮保留原JSON内容/hash；第二组legacy-raw用本卡保留的旧release再次产生真实中断，保存原文件字节副本，然后由新release受控续接。二者provider各总2次，登记进程退出、owner清空，未删除generation或手工改checkpoint。原始文件/进程/来源见A legacy与legacy-raw目录（original-files.zip保留不受Git文本换行归一化影响的原始字节）；B old-artifact/install、upgrade、legacy-resume和provenance关联同一制品。

新装smoke：12 passed＋1 F2 known_gap。合成PDF/解析/翻译→真实Reader，HTTP资产字节匹配manifest；本地合成模型真实工具回合、会话与handoff持久化；两次完整stop/start确认同instance/不同launch、个人记录/Reader/hash/原生会话历史保持。库backup/restore仅作为库能力，不视为整实例恢复。

Excel F1：14 passed，七组CLI案例含旧工作簿/新数据库、Excel-only、三个字段双边冲突、legacy等；冲突反复尝试不改原簿/DB，另一行Excel修改不能部分导入。真实xlsx worker等待用户，两次宿主重启后HTTP详情与原记录仍正确。未打开Office/WPS，也未重复浏览器视觉；HTTP/API不声称新UI能力。

额外失败任务重启探针：在v4do合成library真实worker产生failed，三次launch包含两次完整重启，failed状态和任务JSON字节不变；显式通过原worker排队/执行后到translate_full/waiting_agent。同一parent，见restart-failure.json。

## 失败记录与限制

A保留早期pytest依赖未就绪、缺git identity、CRLF/LF索引状态阻止pack、offline重建wheel与pack重叠导致缺wheel的失败；最终串行构建/pack后才安装。新装固定依赖下载有TLS重试，未降低hash/SSL门槛，安装最终exit0。

重启探针初次标题与provider合成正文不一致，被身份锚点拒绝，见restart-failure-fixture-mismatch。第二次直接在failed后调用ReadingPipeline.advance，后续inspect得到reading_pipeline_state_invalid，见restart-failure-direct-advance；该直接服务调用路径未通过，本卡未修复或宣称支持。最终采用已有真实worker重排/执行路径复验成功。两个早期实例记录保留，未删除失败记录或generation来凑通过；请总控保留该直接调用限制。

未使用真实模型/MinerU、OAuth/凭据、用户库，未改前任务实例；没有真实科学质量、非Windows、Office、全部并发/崩溃窗口结论。客户端未改，未重复视觉验收。004A-G1/取消与F2仍open，004E后续组合必须另验。

## 清理与恢复点

final-stop.json确认v4d/v4do已停止，cleanup.json确认本卡node/python残留0。六场景与legacy记录确认登记worker无存活；制品、合成库和原检查点保留供复核。清理时复合递归缓存删除命令被自动策略拒绝且未执行；改为逐个删除本卡已列明的单个pyc与空目录后完成，没有删除库或制品。

receipt更新review，交付A/B最终报告提交与clean树。原源码回退不回滚数据，回退旧引擎将重新暴露旧running恢复缺口。没有写总控台账、合并main/总控、发布、tag或创建后继。候选通过范围只限本文当前制品和本轮实际证据。

A最终交付提交：a37a415c4bec9a895f1dc1619cfa35081cf85d26。B最终报告提交由交付消息给出；receipt记录制品源码提交，避免自引用SHA。
