# V02-003B：候选制品安装验收交回

2026-09-17，phase=review，结果 **阻塞，未完成安装验收**。不关闭 F1 / V02-003，不更新总控台账。

## 身份与提交

| 仓库 | 工作树 / 分支 | 基线 | 候选来源 | 报告提交 |
|---|---|---|---|---|
| A | `C:/Users/15694/Documents/ChatGPT/deep-literature-engine-v02-003b` / `codex/v02-003b-engine-candidate` | `4e5e6b964ddb8b823be17fb61774f8386bb0ba98` | `ca02db6beb2cf45ad0fd69618313971d0a346c55` | `e3f58d1710413b5c6340fe55974a227b97ee8373` |
| B | `C:/Users/15694/Documents/ChatGPT/deep-literature-v02-003b` / `codex/v02-003b-installed-candidate` | `bde3ee1d79d527fb11c15eda6f46b5522483f02e` | `7a04cf1287f0f4200e736e91618a6b7956949ff2` | 本报告所在提交，完整 SHA 随交接给出 |

两仓初始干净，基线/分支均与派发一致。ownerSession 无可靠当前任务 ID，保留 null。没有使用应用默认 cwd、用户实例或 006B 的资源。A/B 的变更均限制在任务允许路径，生产逻辑和所有运行时/依赖/schema/dataFormat 不变。

## 已完成与实际结果

- A npm `0.2.0-dev.1`、Python `0.2.0.dev1` 元数据先独立提交，再构建。独立 `.venv`，沿用 002 精确 Python 清单及 npm lock，未共享其他任务依赖写入目录。
- A 全量 Python：**515 passed / 3 skipped**，148.80 秒；跳过目录 symlink 权限、Unix zombie 生命周期、需解锁的真实 OS 凭据服务。完整原始证据在 A `docs/codex-v02/V02-003B-evidence/`。
- A wheel/TS/client 构建、41 条默认 Node 离线链、4 条资产链：全部 exit 0；npm pack 54 文件成功。
- 打包 tgz 内唯一 wheel 与 dist wheel 相同；51 个 Python 文件逐字节匹配候选来源，含两个 Excel 修复模块。详见 [artifact.json](V02-003B/artifact.json)。
- B pins 绑定实际 A SHA、tgz SHA256；移除旧 rc.5 URL，使用本地 `inputs/scientific-reading.tgz`；channel 为 internal-development。runtime lock 仅修改插件自身 version/integrity，所有其余条目逐对象比较完全不变。
- B 原始基线模块边界检查通过。只更新产品 VERSION、尚未更新插件 pins 时的诊断全宿主测试为 71 passed / 1 Windows POSIX socket skip，不当作最终候选通过。
- B 最终 pins 下 `modules check` 和 `modules test foundation` 均 exit 1：`engine_pin_mismatch`，模块测试入口在运行测试前被门禁阻止。
- B 最终直接运行 `node --test tests/*.test.mjs`：**70 passed / 1 failed / 1 skipped**。唯一失败是模块身份校验，同为 `engine_pin_mismatch`；未修改断言。原始结果 [final-direct-host-tests.txt](V02-003B/final-direct-host-tests.txt)。直接执行补充定位，不替代模块门禁。
- `modules impact`：tooling/application/engine/foundation；传递调用方包含 skill/lifecycle/releases/workflow/bridge；未归属文件为零。
- 从 B 干净 tracked 来源提交实际运行现有 `package-release.mjs`：**exit 1**，在创建输出目录之前被同一门禁拒绝；`outputs/v02-003b-candidate` 不存在，没有 ZIP 或 BUILD-MANIFEST。来源与空 tracked status 见 [runs.json](V02-003B/runs.json)，堆栈见 [package.txt](V02-003B/package.txt)。

## 精确阻塞与最小追加建议

`src/modules/catalog.json` 仍登记旧引擎，而 `scripts/module-graph.mjs:143` 必须比较以下四项与 pins 一致：

- `modules[id=engine].version`
- `externalEngine.version`
- `externalEngine.sourceCommit`
- `externalEngine.sha256`

任务卡允许 pins 的 plugin 更新，却未允许修改 catalog。现有 package-release.mjs:13 强制调用此检查。因此，继续安装验收必须越出当前 writePaths，不能靠重试解决。本任务未更改 catalog、校验器或打包流程，未临时绕过检查后恢复文件。

建议总控追加 **仅同步上述四个 catalog 身份字段** 的权限，保持所有保护断言、校验器与生产逻辑不变。随后重新提交 B 候选来源，运行边界/受影响测试，再执行原打包、解包校验、全新短路径安装和剩余验收。该建议尚未实施。

## 制品绝对路径与摘要

- A tgz：`C:/Users/15694/Documents/ChatGPT/deep-literature-engine-v02-003b/dsh-external-dsh-scientific-reading-0.2.0-dev.1.tgz`
- SHA256：`fdce0ada390e2f27615c6b6050dbf7ef83c871d6a6304c4235e4414f667c8930`
- B 本地副本：`C:/Users/15694/Documents/ChatGPT/deep-literature-v02-003b/inputs/scientific-reading.tgz`，同一摘要。
- wheel：`C:/Users/15694/Documents/ChatGPT/deep-literature-engine-v02-003b/dist/python/dsh_scientific_reading_engine-0.2.0.dev1-py3-none-any.whl`
- SHA256：`cd6120c31dec1c096ca038ad3775e0456c6c9c45898fb630b3e2a5a9e3e88566`

本地制品未跟踪，交接时必须核对上述 SHA。没有发布、打 tag、推送制品或合并总控。

## 未验证范围、历史与清理

B 打包失败，未创建/安装/启动任何任务实例，未复制缓存、未安装个人 skill。Node 22.22.2 / Python 3.11.16 pins 完全未变，但本轮没有安装解释器证据；实际测试解释器为 Node 24.18.0 / Python 3.11.9。

**未验证**：安装产物 F1 原场景、Excel-only 保存、同字段双改/整批原子性、legacy、重试/两次重启、worker/前端状态与定位，以及安装启动/身份/Reader/笔记/handoff smoke。A 源码/离线通过不能代替这些层次。未新增假装可执行的安装 smoke 报告。

001 的原脚本、夹具和原始 F1/F2 失败证据未修改；F1 本次仍待安装复验，F2 库备份不涵盖整实例状态的局限仍开放。真实 Office/WPS、外部模型/MinerU、用户数据、其他平台、真实登录和整实例恢复均未验证。

所有本轮命令子进程已经退出；没有持久工作台实例或个人 skill 需要清理。A 构建引起的 tracked client 换行变化经无语义差异核对后恢复，未提交。最初 B modules list/context 因缺 acorn 失败，npm ci 后重跑成功；这属于空 worktree 环境准备，未更改依赖。A artifact 脚本初版错误假设 METADATA 换行，改为标准解析后核验通过。

回退仅涉及本任务开发提交；无用户数据回退需求。后继验收与 F1 关闭由总控决定。
