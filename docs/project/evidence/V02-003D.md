# V02-003D：Excel 冲突详情与新候选安装复验

phase=review。F4 详情提示与本卡新候选安装复验通过，待总控独立复核；不自行关闭总控 F1/F2 台账，不合并或发布。

任务上下文固定为总控 7e0a3e4ffbddf21cf5471d0ef14e57411fd1565f。B base c7757c49d509bcec28464120a82cd87d7360458f，A base e3f58d1710413b5c6340fe55974a227b97ee8373。双仓独立分支 codex/v02-003d-conflict-feedback；接收时两仓干净。ownerSession 未可靠解析，保留 null。

A 仅详情客户端与原构建产物、新 DOM 渲染测试/default Node 链及授权元数据。B 仅版本、pins/锁/catalog 身份、引擎追溯文档及本任务测试证据。无 Python/worker/routes/协议/schema 修改。npm dev.2、Python dev1、Node 22.22.2、Python runtime 3.11.16、DSH rc.7 的组合在候选安装核验。

首次 B modules list/context 因新工作树无 acorn、系统 Node 24.18.0 失败；安装锁定 devDependencies 并切换固定 Node 22.22.2 后重跑成功。A 单次编辑脚本漏填 UTF-8 曾在读取时失败，未写入；修正编码后完成，未改产品保护断言。

所有业务使用本地合成数据。旧 003C 证据及 v3c 实例未改；未安装个人 skill，未访问用户凭据或用户库。Office 实际打开不在本卡强制门槛，采用 recording opener，不宣称 Office 验收。F2 整实例恢复仍 open，库备份/恢复不能替代它。

## 候选来源与已完成安装验证

- A 来源：`26abee5f8c8e4f92f26a9b7fba4ddd236e523b05`；A 证据提交 `6283c02a690ff42585f064c1f764b999baa97d7e`。
- B 干净打包来源：`890fba5cb47d7197f844fddf711414bc63694c86`（产品变更提交 `7138e364f82cc5b70081a830366babb5bf4d3584`）。
- A tgz SHA256 `2e1ec6ebbc9ca37d33060d978f5d1f855455bb4423cc5b481957b3da0259bc09`。
- B ZIP SHA256 `a96a0d6bd702e7edd3f9e5327555c978aded3e189ceb06948a9d9da73fcdaca9`。
- wheel SHA256 `b5d6439b1e3e895e0127dac27ec2b12b9f0bdd46d813896a6a90fd990e2b66d3`；Python 分模块仍 dev1。
- 新合成 root `C:/Users/15694/AppData/Local/Temp/v3d`，实例 `8211e3ed-3ba2-43c9-b00b-203a0d911025`。

A 实际 build、42 条默认 Node 离线命令、4 条资产链及 pack 通过；新增 10 组 renderDrawer 节点渲染断言（最小 DOM 实现），修复前同形 HTTP 冲突夹具失败已保留。完整 515 项 Python 测试未重跑，本卡以 Python 源码无差异及 wheel 构建/安装字节核验覆盖来源。

B modules check 10 模块/47 源文件/152 imports；engine 调用方 56 项（55 passed，1 Windows POSIX socket skip）；foundation 及调用方 72 项（71 passed，1 skip）。impact 未归属文件为 0。初次打包因 outputs 父目录不存在而失败，`package-before-output-directory.txt` 保留；补建目录、提交证据后从干净来源重新打包，未修改打包器或门禁。

271 个解包文件校验通过（270 清单文件及 BUILD-MANIFEST 本身）；实际运行 verified-extraction 中 install.ps1，exit 0，skill=null。安装 app 的 41 个 src 文件与 B 清单相同；安装 Python 49 个 scientific_reading 文件与 wheel 字节相同；tgz 的 51 个 Python 文件与 A 源码相同；安装 lib/client.js 与 A 已构建客户端字节相同。所有引擎业务验收使用安装 Python `-I -X utf8`，没有源码 PYTHONPATH。

smoke 12 passed + 1 known_gap（F2）；conflict 14 passed，覆盖七组 CLI 场景、每字段三次独立冲突尝试、整批不写、legacy、实际 worker 与两次宿主重启。详见 `V02-003D/smoke.json`、`conflict.json`、`runs.json` 和命令日志。所有结果均消费上述新候选。

## 实际浏览器与清理

Codex 内置浏览器直接访问本次新实例：首次 `http://127.0.0.1:55125/`，两次宿主重启后分别为 `http://127.0.0.1:54064/`、`http://127.0.0.1:64152/`。以 installed worker 的 derived_pipeline=true 任务关联文献 active_job_id；HTTP 与可见详情分开留证。实际详情可见“Excel 同步已暂停”、原因、当前文献 ID、用户笔记、原工作簿和数据库均保留/整批未导入，以及先备份、核对、保存关闭再重试的建议。

初次加载、关闭重开、整页刷新、两次宿主重启共 5 次详情文本逐字一致；ui-observation.json 保存时间、URL、共同原文及对比结果。普通文献未误显示 Excel 冲突；合成 PDF gate 仍显示启用的 OA 重试按钮和本地 PDF 挂接入口（未触发外部 OA 下载）；从列表实际打开 Reader 并切换中英，正文/公式/目录可见。三个中文字段、其他文献归属、未知/缺少 detail、恶意字符串由 A DOM 渲染测试覆盖，浏览器真实冲突使用 user_notes，不把单个真实场景夸大为浏览器全矩阵。

ui-invariants-before/after 核对回归浏览操作与两次 UI 重启前后，原工作簿 SHA 和个人字段摘要完全相同；快照建立于初次显示/刷新之后。ui-lifecycle.json 保留每次启动的 instance/launch/PID/时间；两次重启 instance ID 相同、launch ID 不同。截图已在任务工具回显作视觉核对，持久证据为实际 DOM/文本，不声称导出截图文件。

首次导航时曾在“文献”选中状态下显示设置卡片，使用现有“进入文献库”按钮进入后正常；刷新与重启后列表直接加载。精确观察见 ui-navigation-observation.txt，仅交回导航现象，不诊断为新回归，不扩展改动。详情加载有延迟，保留初始加载记录，只有实际内容渲染后才计通过。

已通过安装 control.stop 停止实例，final-stop.json 为 stopped；测试浏览器标签关闭，process-cleanup.json 核对本任务 node/python 残留为 0。新合成实例和制品留存供复核。无发布、push、tag、主线/总控合并；未安装个人 skill、未改旧实例或用户数据。

## 交接与复现

证据目录 `docs/project/evidence/V02-003D/`：artifact.json、package.txt、runs.json、install.txt、installed-app.json、installed-wheel.json、installed-client.json、smoke.json、conflict.json、ui-paper-api*.json、ui-observation.json、ui-lifecycle.json、ui-invariants-*.json、final-stop.json、process-cleanup.json、scope-audit.json。A 原始 build/offline/assets/pack/freeze 记录在 A `docs/codex-v02/V02-003D-evidence/`。

主要命令均显式在各自工作树 cwd：A `node scripts/v02-003d-run.mjs build|offline|assets|pack|freeze` 与 `.venv/Scripts/python.exe scripts/v02-003d-artifact.py`；B 固定 Node 22 执行 `scripts/acceptance-v02-003d.mjs check|test|impact|package`、`scripts/modules.mjs test foundation`，解包 install.ps1 不传 InstallSkill，安装后执行专属 smoke/conflict 和 ui-control 脚本。浏览器步骤由 cua_repl 执行，录入为本任务文本证据。

未执行真实 Office/WPS、真实模型/MinerU/OAuth、真实用户库、非 Windows 平台及完整 Python 套件；F2 整实例恢复仍 open。回退为 A 客户端和 B 候选身份的 Git 源码回退，不对文献库数据做回滚。完整 A/B 交付 SHA 在最终交接消息，候选来源 SHA 与制品摘要见上文，报告提交不重建制品。
