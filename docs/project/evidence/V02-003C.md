# V02-003C：候选身份同步与安装验收交回

2026-09-17，phase=review，outcome=needs_followup。catalog 身份门禁已修复，实际候选打包、安装和 F1 数据保护复验通过；**详细冲突的前端呈现仍有缺口，不宣布整体验收全通过、不关闭 F1**。F2 整实例恢复缺口仍开放。总控决定合入与后继任务。

## 身份与范围

- 工作树：`C:/Users/15694/Documents/ChatGPT/deep-literature-v02-003c`；分支：`codex/v02-003c-candidate-acceptance`。
- baseCommit：`8c37fb13d7c6816c1de067d72ef8fef091097ac3`。
- contextCommit：总控仓库 `e13a4bc468217e78fe872c82e28d753af23148ee`；从该提交读取 003C 卡与 003B control-review，旧台账未作为派发依据。
- **B 候选来源：`5a3506aa8429af6ea1db27ecf096f85823fc914d`**；报告提交为包含本报告的独立提交，完整 SHA 在交接消息给出。
- A 固定来源：`ca02db6beb2cf45ad0fd69618313971d0a346c55`；未重建或写入 A/003B 工作树。
- 初始工作树干净。未配置 Git author，核对基线作者 `Codex <codex@openai.com>` 后仅用单次 `git -c user.name=Codex -c user.email=codex@openai.com commit`，未改共享/全局 Git 配置。ownerSession 无可靠当前 ID，保留 null。
- 唯一产品改动为 catalog 中 engine.version 与 externalEngine.version/sourceCommit/sha256 四字段。生产逻辑、保护断言、依赖、pins、版本、schema 均未更改；其他提交文件均为本任务脚本、夹具与证据。001/003B 原始脚本及失败证据原封保留。

## 候选、安装与来源核验

现有 `package-release.mjs` 从上述来源及空 tracked status 打包；明确传入本地 A archive。未修改校验或打包流程。221 个解包文件逐一 SHA 校验通过。实际运行解包目录内 `install.ps1`，未带 InstallSkill，日志记录 skill=null；安装 exit 0。

- B ZIP：`C:/Users/15694/Documents/ChatGPT/deep-literature-v02-003c/outputs/v02-003c-candidate/deep-literature-for-codex-0.2.0-dev.1-win-x64.zip`
- **B ZIP SHA256：`964a772f5242ec0beb1e5f2e9c1a7cfddf3ba3c44b54a61f00da7ab4539fb4d0`**。
- A 输入原件：`C:/Users/15694/Documents/ChatGPT/deep-literature-engine-v02-003b/dsh-external-dsh-scientific-reading-0.2.0-dev.1.tgz`；摘要核对后只读复制到本工作树 `inputs/scientific-reading.tgz`。
- **A tgz SHA256：`fdce0ada390e2f27615c6b6050dbf7ef83c871d6a6304c4235e4414f667c8930`**。
- wheel SHA256：`cd6120c31dec1c096ca038ad3775e0456c6c9c45898fb630b3e2a5a9e3e88566`；安装包内 wheel 与固定 A 摘要一致。
- 独立合成 root：`C:/Users/15694/AppData/Local/Temp/v3c`，实例 ID `4f8cbd14-7706-4db5-98f9-9b67c0acd6cd`。通过解包程序 `initializeRoot` 合法初始化，然后只读复制 001 合成实例的两个下载缓存，各自在复制前后校验 pins SHA；没有复制数据库、凭据或旧安装环境。
- Node `22.22.2`，Python `3.11.16`，A npm `0.2.0-dev.1` / Python distribution `0.2.0.dev1`，B `0.2.0-dev.1`，DSH `0.1.0-rc.7`。
- 安装 app SHA256：`1e4db6237664e5d2` 为槽位前缀，完整值见 [installed-app](V02-003C/installed-app.json)。41 个安装 src 文件逐字节匹配 B BUILD-MANIFEST；49 个安装 scientific_reading Python 文件逐字节匹配 wheel。实际 `scientific_reading.__file__` 位于本 root 的 release/venv/Lib/site-packages。
- 所有引擎命令用安装 Python `-I -X utf8`；没有设置源码 PYTHONPATH。运行时、worker、模型夹具和浏览器均只连接本任务 localhost 实例。安装依赖来自现有锁定 registry，业务验收未调用外部模型/MinerU。

证据：[打包执行和来源](V02-003C/runs.json)、[制品/清单](V02-003C/artifact.json)、[安装日志](V02-003C/install.txt)、[缓存核验](V02-003C/cache-copy.json)、[安装 wheel](V02-003C/installed-wheel.json)、[安装 app](V02-003C/installed-app.json)。补充冲突/来源/界面验收脚本在候选打包后添加，仅消费同一已安装候选；没有重建或修改候选产品。

## 实测结果

| 检查 | 实际结果 |
|---|---|
| modules check | 10 模块、47 源文件、152 imports 通过 |
| modules impact | 改动归 tooling，未归属文件 0；另按外部引擎身份主动测试 engine 调用方 |
| modules test engine | 56 项：55 通过、0 失败、1 Windows POSIX socket 跳过；包含 bridge/application/tooling 相关测试 |
| package / verify / extraction / install | 全通过；安装来自 verified-extraction，不是源码目录 |
| 旧 Excel 未改，DB 三字段更新后刷新 | CLI 和主流程原场景均保留 DB 新值 |
| 仅 Excel 改动 | 三字段全部保存到 DB |
| 双边冲突，三个字段分别验证 | 每种冲突均 pending、updated=0，明确 paper_id/field；Excel 原字节、DB 新值均保留 |
| 整批原子性和重复尝试 | 同批另一行有合法 Excel 编辑，仍未部分导入；每个字段冲突以独立 CLI 进程重复 3 次 |
| legacy 工作簿 | 与 DB 相等可升级基线；有差异 pending，原工作簿和 DB 均保留 |
| 实际 worker / 两次宿主重启 | 初次及两次重启共 3 次独立 worker 进程 exit 2，waiting_user，xlsx_user_fields_conflict；字段和文献定位准确；整批不写、两边字节/值保持 |
| 主流程 smoke | 12 项通过：运行时、启动/身份、重复启动、实际库路由、Reader HTTP 摘要、Excel 笔记、本地模型 sr_library_list 工具闭环和 handoff、两次重启后的记录/Reader/原生历史、F1、库备份/空目录实际恢复和最终停止 |
| worker / HTTP 冲突详情 | 完整传出冲突 reason、paper_id、user_notes 字段和整批未写说明 |
| 前端概要 | 真实浏览器显示 SQLite 文献 2 篇 / Excel 待同步 2 篇、正确资产位置、每篇文献有定位 Excel 按钮 |
| 详细冲突前端提示 | **未通过：文献详情没有呈现 waiting_user / 冲突原因 / 字段，见下节** |
| Excel 行号定位 | 安装 locate() 对实际工作簿返回正确 paper_id/行号；使用任务本地 recording opener，未验证真实 Office 打开/选行 |
| F2 | known_gap；库恢复通过不等于原生会话和 handoff 的整实例恢复 |

[smoke JSON](V02-003C/smoke.json) 共 13 条，12 passed + 1 known_gap；退出 0 仅代表该脚本的有界 smoke 通过，不能代表 F2 或整体验收通过。[conflict JSON](V02-003C/conflict.json) 14 条全通过，第一条包含 7 组 CLI 场景；每次完整命令、退出码、stdout/stderr 均保留。[定位/派生 worker](V02-003C/locate.json) 另验证 derived_pipeline=true 的实际 worker。

## 超出最小范围的前端缺口

为排除“合成 worker 没有关联文献”的误判，使用安装包创建 derived_pipeline=true 的 xlsx_snapshot 请求并运行真实 worker。它正常把 `job_2c6a4b79794e7f4b` 写为该论文 active_job_id，返回 waiting_user / xlsx_user_fields_conflict。HTTP `/sr/api/paper/title_d8b339a0356f` 确认 job 及 `required_input.conflict_details=[{paper_id: title_d8b339a0356f, field: user_notes, code: user_field_conflict}]` 已到达前端接口。

刷新真实 UI 并重新打开该论文详情，仍只显示文献基本信息、摘要状态、精读完成、PDF/Reader/资产操作，没有冲突等待提示或冲突字段。设置页仅显示总待同步数量。原始接口值见 [ui-paper-api](V02-003C/ui-paper-api.json)，实际 AX/详情文本摘录及核对步骤见 [ui-observation](V02-003C/ui-observation.txt)。浏览器 content.export 不受支持，因此证据文件明确为实际观察文本，未伪称保存了截图文件。

该缺口要改变 A client 的生产渲染逻辑，超出本卡四字段及验收写入范围。未修改 UI、worker、保护断言或接口来绕过门槛。建议总控明确是否要求详细冲突可见，或单独派发 UI 最小修复；本任务不自行认定该缺口豁免，不关闭 F1。已有数据安全复验结论独立保留。

## 运行方式、限制与清理

所有命令 cwd 显式为本任务工作树。主要命令：

```powershell
npm ci
# Node 22.22.2 放在该命令进程的 PATH 首位
node scripts/acceptance-v02-003c.mjs check
node scripts/acceptance-v02-003c.mjs test
node scripts/acceptance-v02-003c.mjs impact
# 提交候选来源后，tracked status 为空
node scripts/acceptance-v02-003c.mjs package
node scripts/acceptance-v02-003c-prepare.mjs
powershell.exe -NoProfile -File <verified-extraction>/install.ps1 -Root C:/Users/15694/AppData/Local/Temp/v3c -PluginArchive <verified-extraction>/inputs/scientific-reading.tgz
<installed-node> scripts/acceptance-v02-003c-smoke.mjs C:/Users/15694/AppData/Local/Temp/v3c docs/project/evidence/V02-003C/smoke.json
<installed-node> scripts/acceptance-v02-003c-conflict.mjs C:/Users/15694/AppData/Local/Temp/v3c docs/project/evidence/V02-003C/conflict.json
<installed-python> -I -X utf8 scripts/fixtures/v02-003c/provenance.py C:/Users/15694/AppData/Local/Temp/v3c
<installed-python> -I -X utf8 scripts/fixtures/v02-003c/locate-probe.py C:/Users/15694/AppData/Local/Temp/v3c
```

实际执行 prepare/artifact/installed-app/ui-api 时脚本先置于 outputs，内容相同的交付副本现位于 scripts/acceptance-v02-003c-*。首次 modules list/context 因新 worktree 缺 acorn 且系统 Node 24.18.0 失败，npm ci 后改用 Node 22.22.2 重跑通过；不将准备阶段失败隐藏为候选失败。未更改锁文件。

未验证：原生 Office/WPS 打开定位、真实模型/科学内容质量、真实 MinerU/OAuth/凭据、用户库、非 Windows 平台、整实例恢复。Parser/翻译来自合成夹具，Reader 使用实际 normalizer/renderer/publish 流程；不宣称全自动后台全文流水线全覆盖。

个人 skill 未安装，真实实例/应用默认 cwd 未写入；没有发布、tag、push、合并 main 或改总控台账。最终通过本实例安装 CLI stop，临时浏览器标签已关闭；[final-stop](V02-003C/final-stop.json) 为 stopped，[process-cleanup](V02-003C/process-cleanup.json) 按本任务绝对运行时路径核对残留进程为 0。测试实例和未跟踪制品保留供复核，不包含真实用户数据。

回退仅 catalog 元数据和本任务测试提交；不对任何用户记录做回滚。
