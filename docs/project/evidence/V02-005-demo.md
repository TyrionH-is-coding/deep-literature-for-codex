# V02-005 本机演示命令

以下仅适用于本任务独占实例，端口不能固定复用。开发工作目录是 `C:/Users/15694/Documents/ChatGPT/deep-literature-v02-005`。不操作旧 dsh reader、004K 或用户实例。

```powershell
powershell.exe -NoProfile -File 'C:/Users/15694/Documents/ChatGPT/deep-literature-v02-005/scripts/v02-005-reopen.ps1'
```

此命令只启动原实例、可信校验并输出三个 `readerUrl`。在 Codex 内置浏览器先打开本次 `start.url + /__workbench`，核对 instanceId 为 `4495c8ba-c27b-4bdb-9bf8-54551fc0171f`，再打开返回的 Reader 链接。公式样例切“中英”，点核心结果“定位原文”，可看到公式与中文；图表样例点图/表放大；上下标样例显示 H₂O、x² 及普通单词。首次 DSH 内部测试提示是非绑定说明。本次重启前端口60302、重启后57913仅是历史证据。

原生库内路径也已实际验收（本地 mock 启动端口51011，历史端口不要复用）：打开本次 `start.url`，选会话“文献 · V02-005 synthetic normal formula-outline”（session `session-af4566ce-801b-4146-ba3b-043e6da9588c`），点“文献”，分类选公式样例，再点“打开 HTML”。该会话已有正式 completed 的合成确认消息，查看已有会话不需要再发模型请求。点击“定位 Excel”时，本机实际反馈为“已打开文献总表，请按论文名查找对应记录。”，这是回退打开总表，未验证桌面选中行。截图10–13及对应浏览器文本保留。临时本地 mock profile 已恢复为安装原值 `[]`，未重新调用模型。

保存记录已有演示结果：Excel 文件 `C:/tmp/v005/instance/library/library/scientific-reading.xlsx`，“文献”sheet 的公式样例行，三字段为“合成思考：公式与目录已核对 / 部分理解 / V02-005：重启后继续阅读”。如以后用 Excel 更改，保存并关闭后执行：

```powershell
$root='C:/tmp/v005/instance'
$i=Get-Content -Raw "$root/installation.json" | ConvertFrom-Json
& $i.python -I -X utf8 -m scientific_reading --data-root "$root/library" xlsx-import-user-fields
& $i.python -I -X utf8 -m scientific_reading --data-root "$root/library" xlsx-refresh
```

可信只读状态：创建 UTF-8 请求文件再 call，不使用旧端口或直接写库。

```powershell
$root='C:/tmp/v005/instance'
$i=Get-Content -Raw "$root/installation.json" | ConvertFrom-Json
$request='C:/tmp/v005/check-failure.json'
[IO.File]::WriteAllText($request,'{"action":"task","payload":{"taskId":"task-1544fc72-b89a-4f62-8877-3cf0d0a2701e"}}',[Text.UTF8Encoding]::new($false))
& $i.node (Join-Path $i.app 'src/cli.mjs') call $root $request
```

失败演示 task 为 `task-1544fc72-b89a-4f62-8877-3cf0d0a2701e`，parent `job_d3cbdf77f2ed271b`。真实记录中的第二次停止 revision3、acknowledgedRevision3、businessStatus failed；恢复请求为 `{"action":"resume","payload":{"taskId":"task-1544fc72-b89a-4f62-8877-3cf0d0a2701e","idempotencyKey":"v005-explicit-safe-recovery-r3","resumeStopped":true,"expectedRevision":3,"input":{}}}`。实际 worker 推进至 revision4、waiting_agent/translate_full_read，source SHA 保持 `8d63d012b941c836b20f0ff5f8508c72f40822fbbe3c8084567df6f15b7cccc1`。**该请求是已执行证据，不是当前 gate 可再提交的输入**；当前后续必须读取实际 gate 并提交 `full-translation-v3`。

重放脚本仅用于新的隔离准备过程，不应对已保留实例从头重跑。已执行命令与退出码见索引；正常链首次旧合同失败记录及所有夹具修订保留，最终代码可从正常路径正确运行。安装命令（已执行）为解包后的原 `install.ps1 -Root C:/tmp/v005/instance -PluginArchive C:/tmp/v005/package/deep-literature-for-codex-0.2.0-dev.4-win-x64/inputs/scientific-reading.tgz`，未加 InstallSkill。

演示结束只停止本实例：

```powershell
powershell.exe -NoProfile -File 'C:/Users/15694/Documents/ChatGPT/deep-literature-v02-005/scripts/v02-005-reopen.ps1' -Stop
```
