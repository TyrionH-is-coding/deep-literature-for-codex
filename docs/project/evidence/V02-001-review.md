# V02-001 总控复核

日期：2026-09-17。交付提交：`86d7833cbc22bfaaa8f4694318f59bb3706acab7`；验收候选业务代码基线：`ac5248c8797138409aadd23ec846361ef300d67c`；对照：`e7160ebda22a196f3b8dcf7a09cb94d137b7de0e`。

## 结论

接受“隔离验收与问题定位”交付，资料集成到 `codex/v02-control`；不据此声明产品全通过，也不合入 main 或发布。V02-002 可以进入 ready，其工作限定为固定引擎的源码、接口、构建来源和测试基线审计。

## 复核依据

- 已阅读[完整验收报告](V02-001.md)、最终候选/rc.6 JSON、平台与包清单结果、最终审计记录以及实际夹具实现。
- Git 差异仅含验收脚本、合成夹具、证据与任务卡，`src/`、`runtime/`、`oauth/` 无变化；任务分支交付工作树干净。
- 候选两次重启记录包含真实合成笔记与 Reader 摘要，launchId 不同、instanceId 一致；早期失败夹具没有被当作通过证据。
- 平台报告记录 26 项检查通过；补充候选报告 13 项检查中 11 项通过、2 项失败；rc.6 对照 8 项中 6 项通过、同样 2 项失败。它们的范围不同，不相加宣传为整产品通过率。
- 两套恢复库的非 job 资产无摘要差异，恢复后的 Reader 发布合同验证成功；停止与测试 profile 恢复有证据。
- 本次是交付复核，不是重新执行整套真实安装验收。完整命令及原始产物见原报告；后台自动流水线、活跃作业中断恢复、真实服务和浏览器视觉交互仍属于未验证范围。

## 保留问题

总控本轮实际执行：`node --check scripts/acceptance-v02-001.mjs`、`npm run check:modules`（10 模块、47 生产文件、152 导入）、`npm run test:modules`（20 通过）、Git 差异与冲突检查。相对原总控 `56bdffa`，`src/runtime/oauth` 无改动。未重跑已完成的真实隔离安装与外部依赖下载。

| ID | 事实与归因 | 下一步 | 是否阻止 002 |
| --- | --- | --- | --- |
| F1 | 旧 Excel 将较新的三项 SQLite 个人字段覆盖，refresh 返回 success；原始 rc.6 同样复现。直接 SQL 只用于建立精确冲突，未证明所有 UI 编辑路径均触发。 | 进入 V02-003，先定义权威来源/冲突合同，再独立修复；数据保存验收通过前不视为解决。 | 否；002 只审计该接口和测试缺口 |
| F2 | 文献库/资产可以恢复；备份条目没有原生会话和 handoff 状态，原始 rc.6 相同。这是整实例恢复范围缺口，不是库备份承诺范围内的数据丢失。 | 进入 V02-006，单独设计状态域、身份重绑定与恢复演练。 | 否；002 只登记库备份合同 |

F1/F2 均保持开放。缺陷修复不属于 001 或 002 本轮范围。后续实现及 v0.2 发布仍需要相应修复/恢复证据。

## 任务追溯限制

应用任务列表与归档列表暂未返回与创建请求对应的真实开发 session ID，因此不编造 `ownerSession`。交付身份以实际 worktree、完整 commit、固定候选制品及证据为准；保留原 `clientThreadId` 供后续关联。任务完成事实来自上述交付，不再沿用旧台账“等待启动”的过期状态。
