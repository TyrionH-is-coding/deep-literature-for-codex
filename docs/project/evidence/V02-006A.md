# V02-006A 交付报告

日期：2026-09-17；状态：review，等待总控复核。交付仅为 [完整实例恢复方案](../design/instance-recovery.md)，不代表恢复功能或 S5 已通过。

## 身份与范围

- 工作目录：`C:/Users/15694/Documents/ChatGPT/deep-literature-v02-006a`。
- origin：`https://github.com/TyrionH-is-coding/deep-literature-for-codex.git`。
- 分支：`codex/v02-006a-recovery-design`；baseCommit/contextCommit 均为 `d4d0cf3af2b5da3bd0e1ade7bfc6645d52cb026c`；开工 HEAD 相同，初始状态干净。
- 引擎只读目录：`C:/Users/15694/Documents/ChatGPT/deep-literature-engine`；HEAD 为 `8e00b334389cd90721b7d404013a07687753aa5c`，检查时工作树干净。未读 V02-002 开发目录。
- [回执](V02-006A-receipt.json) 开工已写 running，交付改 review。未取得可可靠确认属于本任务的真实 Codex ID，ownerSession=null；未将来源任务 ID 或目录名当作自身 ID。
- 仅三个获准文档有改动；没有生产接口/格式变化，没有台账、PROJECT 或用户实例修改。未集成、合并、发布或创建任务。

## 验收对应

| 任务要求 | 文档交付与边界 |
| --- | --- |
| 三域位置及事实 | 方案第 1 节列源码符号、固定引擎行号与 001 原始 JSON；库备份不含 DSH/handoff 的 F2 被保留为现状缺口 |
| 一致性、摘要、失败中止 | 第 3–4 节明确持续冻结、三域清单、独立目标及 ready 门禁；指出现有维护锁/库备份不足，属于待实现要求 |
| 身份、路径、绑定及凭据 | 第 4–5 节定义新 instanceId、新 workspace、稳定 session/业务 ID、路径白名单、未完成任务对账及凭据排除；DSH 格式未证实，不假装可直接复制 |
| 故障注入 | 第 6 节覆盖缺文件、摘要、版本、重绑定、部分恢复、中断、并发、未知投递；全为未来验收计划 |
| 最小方案与拆卡 | 第 2 节比较三种方案，推荐停机三域包；第 7 节给出 R1–R5 单目标草案，无任务状态变更 |
| 验证与交付 | 本次执行文档链接、JSON、源码事实与 Git 差异检查；未运行产品测试，最终 SHA 随交接消息返回 |

## 实际检查记录

全部工作台命令显式使用上述工作目录；引擎命令显式使用固定引擎目录。没有在应用默认 cwd 执行仓库操作。

1. `git remote -v`、`git rev-parse HEAD`、`git branch --show-current`、`git status --short`：与派发身份一致，初始干净；引擎 HEAD/status 同样核对。
2. 阅读 AGENTS、PROJECT、任务卡、auto-loop、session-protocol、releases/workflow/lifecycle 模块说明及公共入口。按直接关系补读 foundation identity/environment、维护/进程控制、handoff/workspace、library-transfer、pins；固定引擎只读 library_backup.py/data_guard.py 的相关实现。
3. `npm run modules -- list` 与三个 `context releases/workflow/lifecycle` 命令均因缺少 `acorn` 失败（系统 Node 24.18.0）；没有 npm ci、依赖安装或升级。改用模块说明、公共入口和 catalog 静态定位；不把这些失败记录为检查通过。
4. 阅读 001 报告 F2、v02-probe.json 的 backup/members、v02-rc6.json 与 final-audit.json。结构化复核 backup 文件数、35 个非 job 资产零差异、两类缺失布尔值及 rc.6 对照。没有访问原测试实例或备份 ZIP；这些属于已提交原始证据的复核，不是本次恢复实测。
5. 本地 Node 内存脚本解析两个新 Markdown 的全部链接：相对链接检查文件存在；两个引擎固定提交链接按 URL 路径映射到已核对 HEAD 的本地源码，检查存在。未联网验证 GitHub 链接可达性。回执 JSON 解析并核对 taskId/phase/基线。
6. `git diff --check`、`git diff --cached --check`、暂存文件白名单检查：提交前执行。对 src/runtime/oauth/PROJECT/tasks.json 的基线差异为空；无源码或台账改动。

定向阅读中曾探测不存在的 `backup.py`、`foundation/core.mjs`、configuration/authentication 目录，以及一次 Windows 不接受的 rg 通配符路径；均为只读失败，随后通过 `rg --files`、公共入口及实际路径定位，未因此扩展为全仓库扫描。

## 未验证、风险与交接

没有运行产品服务、模型、MinerU、真实登录、库备份/恢复、宿主会话导入、故障注入或跨平台测试。没有执行 modules test/check/impact（模块辅助工具依赖缺失，纯文档用差异和链接检查替代）；001 的历史通过项不算本次执行结果。

最小设计仍依赖后续明确 DSH rc.7 会话/队列迁移合同、持续跨域冻结与恢复启动门禁，以及会话内容包含凭据时的可导出边界。无法安全导入时整实例恢复应失败，不得降级后宣称完整成功。后续实现卡需按 V02-006 正式依赖派发；最终候选制品恢复演练与 S5 结论仍未完成。

回退点为上述 baseCommit；撤销本任务文档提交即可，没有用户数据或运行状态需要回滚。最终提交仅包含方案、本报告、review 回执，由总控独占台账和集成操作。
