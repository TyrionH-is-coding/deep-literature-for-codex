# V02-002 总控复核

交付提交：引擎 `d26cb9e88d0f7884db8e69a5a2d18286a62aaf83`。来源基线：`8e00b334389cd90721b7d404013a07687753aa5c`。结论：接受来源、接口和测试基线审计交付，集成到引擎 `codex/v02-engine-baseline`；不宣称产品全部测试通过。

## 实际复核

- 交付仅增加 `docs/codex-v02/` 及 `scripts/v02-audit-*`，生产 Python/TS/client、package.json/锁文件无变更，交付 worktree 干净。
- 阅读主报告、接口/测试地图、来源比较脚本与逐文件分类、JUnit、逐命令退出码及额外导航失败日志。
- 总控在内存重新读取固定 tgz，校验 SHA，并将 wheel 的全部 50 个 Python 文件与固定 Git blob 比较；文件集合相同，换行规范化后全部一致。没有解压到用户实例或改动产物。
- 总控核对 test-runs 的 46 条成功记录与 package.json 的 44 条默认 Node 命令一致；其余两条是构建和 Python。JUnit 为 487 项，其中 484 通过、3 跳过、无失败/错误。该核对是证据审查，不是重复跑全部测试。
- 第一次总控内存脚本以系统默认编码读取中文 package.json 失败；显式 Python `-X utf8` 后全部核对通过，属于复核脚本环境问题。
- 报告中的压缩包字节差异已限定到换行、wheel 元数据/生成器与时间戳，JS 编译输出对应；没有伪称历史构建供应链得到签名证明。

## 保留事项与后继

F1/F2 仍开放。新增 F3：默认测试链之外的 `tests/navigation-contract.mjs` 首个断言缺少现行 `journal`/`search_matches` 字段而失败；其后断言未执行，不能推断余下全部通过。将它交给 V02-002B 单独核实并纳入默认链，保持脱敏/路径/方法等保护断言，不降低断言来追求通过。

F3 不阻断 F1 的 Python Excel 修复：来源对应关系已核实，F1 已有固定基线复现与相关默认测试。002B 只写导航测试和 package.json 的测试脚本，003A 只写 Excel 模块及其测试；独立引擎 worktree，无共享运行实例。允许两项并行，合入串行并验证组合。

003A 采用逐字段三方比较，保留 SQLite 与 Excel 的冲突证据，保持 schema 4 和现有 pending/错误反馈路径。源码修复通过不等于工作台安装制品已更新；003 工作域还须安排新制品集成及原场景复验后，才能关闭 F1。

原始证据位于引擎该提交的 `docs/codex-v02/V02-002.md` 和 `docs/codex-v02/evidence/`；三项跳过、真实服务/Node 22/跨平台未验证等边界继续保留。
